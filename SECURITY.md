# 安全（Security）

ShuyoNote（数友笔记）的**安全模型**：本地优先，默认数据留在本机；多设备 / 团队实时同步依赖独立的商业授权组件 `shuyonote-sync-server`（私有仓库，另见其发布说明）。本仓库（客户端）的安全约定如下。

## 安全模型概述

- **本地优先**：笔记、附件默认只写本机磁盘（桌面 `SQLite`；Web `sql.js` + IndexedDB）。数据不因同步而离开你的设备，除非你主动配置同步。
- **可选端到端加密（E1）**：工作空间库（SQLCipher）+ 附件（会话密钥）静置加密；密钥只驻留会话内存，不落盘。默认关闭，可在应用内配置。
- **同步加密（E2E）**：开启加密后，同步载荷在**客户端**加密，服务端只存密文。
- **内容寻址附件**：附件按内容 SHA-256 去重与校验，避免重复存储。
- **凭据**：同步令牌等敏感信息存于本机（不在公开仓库中写入任何真实密钥 / 口令）。

## 报告安全漏洞

请**不要**在公开 issue 中提交安全漏洞详情，以免在修复前被他人利用。请通过私有通道报告：

- **GitHub**：在仓库的 **Security → Report a vulnerability**（[`ShuyoNote/ShuyoNote`](https://github.com/ShuyoNote/ShuyoNote/security/advisories/new)）创建私有安全公告。
- **GitCode**：本仓库在 [gitcode.com/shuyo-cn/ShuyoNote](https://gitcode.com/shuyo-cn/ShuyoNote)，如需私信或其他方式，请通过项目主页联系维护者。

请附上：受影响版本、复现步骤（最小示例）、影响说明、可能的缓解措施。若你遵守**负责任的披露**（给维护者合理修复时间，再公开），我们会尽力配合并在修复后致谢。

## 我们的处理约定

- 收到报告后确认范围并尽快给出**修复 + 发布**，或如果该问题本来就需要一段时间，会先与你商定一个发布 / 披露时间。
- 完成并发布修复后，会在此仓库公开**摘要**（影响、修复版本、致谢），但**不会公布利用细节**。
- 涉及安全模型的**详细审计与利用细节**保留在私有仓库 / 内部文档，不随本公开仓库发布（本仓库不含攻击利用步骤）。

## 版本与支持

客户端以 **AGPL-3.0** 开源。安全修复会随常规版本发布并写入 [`CHANGELOG.md`](CHANGELOG.md)。请使用最新稳定版本，以获取已修复的安全更新。
