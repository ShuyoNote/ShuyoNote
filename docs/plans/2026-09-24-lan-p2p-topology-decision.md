# 局域网 P2P：**同一网段内多台设备**选哪一档（甲 / 乙 / 丙）—— 决策简报（2026-09-24）

> 上位：[同步服务端数据边界](../sync-server-data-boundary.md)（E2EE 与「服务端哑且盲」的总口径）、
> [SYNC](../SYNC.md)（三条通道与 `seq` 的角色）、[实时协同分析](../realtime-collab-analysis.md)、
> [CRDT 全上线冲刺](2026-09-23-crdt-full-launch-sprint.md)（★ 正文已经 CRDT ⇒ **合并不再需要权威序**）。
>
> ⚠️ 本文**只列决策与改动面，不写码**（第一片的落地记录见 §12）。**拍板之前**的约束是：
> ① 不启动任何一档的实现；② 不动 `changes` / `seq` / wire；③ 不回答「成员凭证的具体形状」
> （那与「换设备配对」是同一件事，必须同批设计，见 §6 的 B 片）。
> —— **2026-09-24 已拍板**（§9）：① 按 §10 的次序解禁（甲-1、B 片、丙第一片均已落）；
> ②③ 这两条口径**不变**，仍然照守。

## 0. 一句话

「同一局域网内能不能多台 P2P」要拆成两问 —— **台数**（今天就没有上限：N 台各自与中枢换增量，
发现层只是一张对端表）与**网状直连**（新问题）。而网状直连的卡点**只有一个**：
**页级 LWW 的权威序（`changes.seq`）只能由一个地方发**。

正文那半边**已经不需要它**（Yjs 自己收敛，F4）；剩下的非正文半边（整页 14 列 ＋ 删除墓碑 ＋ 附件元数据，
F3）**仍然需要一个序** —— 这就是甲/乙/丙 三档的全部差别所在。

## 1. 事实（每条带证据，可当场复核）

| # | 事实 | 证据 |
|---|---|---|
| F1 | **客户端不留账本**：apply 路径把远端变更**只写进 `pages` / `attachments`**，**不写进 `changes`** ⇒ 客户端的 `changes` 表 ＝ **自己的待推队列**，不是账本 | `src-tauri/src/sync.rs:2412`（`fn apply_pulled_changes`）全篇没有 `INSERT INTO changes`（那种语句只在 `:114` 的**记录**路径上）；`changes` 的定义在 `src-tauri/src/db.rs:769` |
| F2 | **推完就没了**：水位一推进，本地那些 `changes` 行只是「已推过的历史」；**权威账本在服务端** | `sync.rs:313`（`unsent_page_change_count`）与 `sync.rs:330`（`discard_unsent_page_changes`）都以 `pushed_watermark_for_page`（`sync.rs:293` ⇒ `sync_profiles.last_pushed_seq`）为界 |
| F3 | **过网实体只有 4 种组合**，其余一律 `_ => {}` 静默忽略：`page` upsert（**整行 `PageDetail`**）/ `page` delete（墓碑）/ `attachment` upsert / `attachment` delete | `sync.rs:2439`（`match (change.entity, change.op)`）→ `:2527`（`_ => {}`）；整行定义见 `src-tauri/src/models.rs:20`（`id, workspace_id, parent_id, title, content_json, content_text, cover, icon, cover_height, cover_pos, kind, sort_order, created_at, updated_at` ＝ **14 列一版**） |
| F4 | **正文不需要权威序**：每页持久化**同一血统**的 CRDT 状态；收侧「收到就收进旁路表」，真正的合并由 WebView 里**那一份唯一实现**在打开页面时做 | `sync.rs:2452` 调 `absorb_incoming_crdt_state`（定义在 `:175`）；`page_crdt` / `page_crdt_pending` 见冲刺 §11–§14 |
| F5 | **发现层是三档共用的同一块砖**（mDNS 通告 ＋ 对端表：指纹 / 地址 / 端口 / 服务哪些空间 / 是不是中枢），与后文三档**解耦** | 本文 §3 的三档都建立在它之上；它本身是唯一「本机可验」的新基建 |
| F6 | ★ **许可约束**：客户端 **AGPL-3.0 开源**，`shuyonote-sync-server` 是**私有商业授权组件** ⇒ **不许**把服务端内嵌进客户端（否则被迫开源，或需一份例外授权） | `README.md`「多设备同步」章：客户端 AGPL-3.0；服务端为独立商业授权组件、私有仓库 |
| F7 | **Web 版「不提供」多设备同步**（**产品决定** 2026-09-15 裁定；2026-09-25 owner 再次确认）。⚠️ 口径是「不提供」，**不是「没有实现」**：`web.ts` 里那套 TypeScript 同步引擎是**两份实现之一、完整存在、且按裁定保持** —— 说成"没实现"与代码相反，还会诱使下一个人把它当死代码删掉。用户可见表现＝配置区在非 Tauri 平台**整体置灰、不可交互** | `docs/web-sync-boundary.md`「结论先行」（该文自注：原文写"Web 侧根本没有实现同步引擎"**与代码相反**，已改正）；`README.md` §「多设备同步」首行「**仅桌面版**」；`SyncPanel.tsx:769` 的 `` isDesktopPlatform() ? "" : " is-disabled" `` ＋ 同文件 `:720` 的提示行 |
| F7b | ✅ **已处置（2026-09-25，owner 取 B：留痕，不静默）**：桌面**照旧有意忽略**不认识的变更类型（前向兼容），但**不再无声** —— `sync.rs` 的 `_` 支把 `entity:op` **去重**收进 `PulledApply.unrecognized`，`do_pull` 打一行 `[sync] 收到 N 种本端不认识的变更类型…`。判据 `unrecognized_entity_kinds_are_ignored_loudly_and_never_wedge_the_cursor`：① 两种搭配被数出来（去重、按首现顺序）② 认识的照常落库、不认识的**一条都不落** ③ ★ **游标照旧走到批尾**（否则一种读不懂的搭配会把它后面所有变更**永久堵死**）④ 它们**不是失败**（不归档、不报冲突）。⚠️ **变异实测**：把 `_` 支改回无条件忽略 ⇒ 该判据红（`left: []` vs `right: ["prop:upsert","attr:delete"]`），已还原。**仍然没做**的是把三类对象**正式纳入协议**（＝原选项 C，与「Web 同步要不要开」绑在一起，未排期） | 发：`src/lib/platform/web.ts:1897`/`:1906`（`page_tag`）、`:2544`/`:2553`/`:2557`（`attr`）、`:2574`/`:2579`（`prop`）；收：`src-tauri/src/sync.rs` 那个 `match`（只认 `("page","upsert")` / `("page","delete")` / `("attachment","upsert")` / `("attachment","delete")`，`_` 支现在**留痕**） |

## 2. 三档对照

| | **甲** 单中枢 | **乙** 谁在线谁当 | **丙** 真网状 |
|---|---|---|---|
| 台数 | 无上限（N 台各自连中枢） | 无上限 | 无上限 |
| 号牌（权威序）谁发 | 你指定的那台常开设备（＝今天） | 当期那一台（要**交接**） | **没人发** —— 「时间 ＋ 设备号」自己算（HLC） |
| 要搬账本（log）吗 | 不搬（它就是服务端） | ★ **要搬** —— 见 §3 | **没有账本这回事** |
| 中枢 / 某台不在线 | 同网段同步**暂停**（不丢数据，等它回来） | 换一台当 | 照常 |
| wire 协议 | **零改**（同形状 `/push`、`/pull`） | 加选举 ＋ 交接 ＋ 交接期去重 | 换定序（HLC）＋ 记录带版本戳 |
| 本地新增持久状态 | **什么都不用** | 中枢身份 ＋ 交接状态 | 每条记录的版本戳（HLC ＋ 设备号） |
| 「同一笔经两条路到达」 | 不会发生 | **会**（交接期两条账本） | 靠幂等 ＋ **按内容**记水位挡掉 |
| 冲突 / 裁决 UI（`dirty`、待取回存档） | **一字不动** | 要跟着交接改 | **要重做**（那套是为 `seq` 写的） |
| 本机可验 | ✅ 两个进程 ＋ 环回 | 🔶 交接要专门造场景 | 🔶 N 对端可模拟，但要新造仿真夹具 |
| 量级 | **一片** | 「中等」偏大（§3） | 阶段 2 那半 ＋ 裁决重做 |

## 3. 乙的洞：它要一份**客户端手上没有**的账本

乙要「谁在线谁当」，可新中枢**手里没有账本**（F1/F2：账本只在服务端/旧中枢上，客户端推完就不留全量 log）。交接于是只有三条路：

1. **旧中枢必须在场**对账 ⇒ 退化成甲，还多了一堆交接代码；
2. **让每台客户端留全量 log** ⇒ 新表 ＋ 存储随历史增长 ＋ 清理策略 ＋ 水位语义全变；
3. **改成不靠 log** ⇒ 那就是丙。

⇒ **乙是「两头都要」的那一档**：留着序号模型的负担，又额外背上账本归属与交接。
除非有硬理由必须保住 `seq`，**建议跳过乙**。

## 4. 丙其实在**删**东西，不是在加

丙去掉两样：**号牌**（序权威）与**账本**（log）。代价收在三处新增 ＋ 一处重做：

- **HLC 判序** ＝ 纯函数 ＋ 两侧各一份判据（本仓最擅长、最容易本机自验的那一类）；
- **每对端一个水位**（「我见过 A 到某时某刻」）＝ 现有 per-workspace 水位（`sync_profiles.last_pushed_seq`）推广成 per-peer，是**加一列**，不是新机制；
- **附件对端选择**（内容寻址已在，缺的只是「跟谁要哪份」）；
- ＋ 一件要诚实说的：**冲突 / 裁决那一套 UI 是为 `seq` 写的，丙要重做** —— 这是丙真正的大头。

## 5. 建议

**甲做阶段 1，丙做阶段 2 的目标，乙跳过。** 三条理由：

1. **甲当场兑现「不依赖外部服务器」**，且协议零改、一致性零改、判据可本机自验（两个进程 ＋ 环回）；
2. **甲的发现层是丙原封不动要复用的那块砖**（F5）⇒ 先做甲**不浪费**；
3. **丙是唯一「减少机制」的那一档**（删号牌、删账本）⇒ 长期维护成本反而低；把 HLC ＋ per-peer 水位
   当成「阶段 2 的第一片」，与 CRDT 冲刺的节奏同形（先纯函数 ＋ 判据，再接线）。

**B 片（二维码 / 短码 PAKE 搬公开材料）与这三档全部正交**，应当独立并行 —— 它是
「零服务器 ＋ 立刻可验」的那一件，而且**甲和丙都要用它做配对**（F6 之下，配对的信任根就是
E2EE 的公开材料本身：有空间钥匙才读得懂内容，所以局域网链路**不需要**公网令牌那套）。

## 6. 每档要动手前，先立哪条承重判据

| 档 | ★ 承重判据（退化了就变红） |
|---|---|
| **甲** | ① **真的走了局域网**：profile 解析结果断言为发现到的局域网地址，而不是公网 URL；② 中枢不在 ⇒ 状态行如实显示，且**不静默**改走公网（除非用户开了回落） |
| **丙** | ★ **没有任何设备发号牌也能收敛**：N 个对端**乱序 ＋ 重复**投递后，两侧投影逐字节相同；且**去掉中枢进程**之后仍然收敛 |
| **乙**（若仍要做） | ★ 交接期「同一笔改动只被计一次」—— 这条不立，乙不能开工 |

## 7. 边界（写进产品口径，不留白）

- **不承诺**：跨网段 / NAT 穿透 / 中继 / Web 参与。
- **够不着就回落**：局域网 → 手填地址 → 公网服务器；**回落必须显式可见**（状态行写清「正在走公网服务器」）。
  ⇒ 「没走成直连」是一个**可断言的结果**，不是失败。

## 8. 本文不做

- 不改任何代码、不动 schema、不动 wire 格式；
- **不启动**甲 / 乙 / 丙 任何一档的实现（等批）——⚠️ 这一条是**出稿当时**的边界；2026-09-24 拍板后按 §10
  的次序解禁（落地记录见 §12）；
- 不回答「空间级成员凭证的形状」（与 §5 的 B 片同批设计）；
- 不改变现有的同步闸门口径（个人空间未加密不许绑同步），三档都照旧过闸门。

## 9. owner 拍板栏

**✅ 2026-09-24 owner 拍板：按建议执行。**（口述原话：「按建议拍，然后再做『扫二维码换设备』的小活」）

- **阶段 1 取甲：✅**
- **阶段 2 目标取丙：✅**（留后，不排期）
- **跳过乙：✅**
- **B 片（二维码 / 短码 PAKE 换设备）：✅，顺序在甲之后**
- 备注：顺序是 **甲 → B → 丙（目标）**。
- **附（2026-09-25 追认）：Web 版产品上「不提供」多设备同步 —— 与 2026-09-15 裁定一致，见 F7。**
  ⚠️ 这一条**只**确认「Web 不开同步」，**没有**确认 F7b（协议里那三种实体要不要进、
  桌面静默丢弃要不要改成留痕）—— F7b 当时仍待定，别把这次追认读成把 F7b 一起拍了。
- **附（2026-09-25，F7b 处置）：取 B —— 留痕，不静默。**（理由：本仓已经为**同一形状**的 bug
  付过一次账 —— 冲刺 §11.4 桌面静默丢掉 `crdt_state`，专门开了一轮才查出来；而 `_ => {}`
  是同一张脸、同一处 `apply` 路径。）
  口径：**照旧忽略**（前向兼容），只是**不再无声**；并把「把三类对象**正式纳入协议**」
  （＝原选项 C）明确**留待**「Web 同步要不要开」那件事，**未排期**。落地见 F7b 行。
- **附（2026-09-25）owner 改口径：直接向丙进化 ⇒ 甲-2 冻结。**（原话：「我希望直接向丙进化」）
  ⇒ 不再为丙要删掉的那一半（发号牌 ＋ 账本）投资；甲-2 的**价值目标**（不用装服务端）
  改由 **§13 的丙-③** 兑现。分片、代价与判据见 §13。**甲-1 与 B 片不受影响**（都已落）。

## 10. 落地顺序（拍板之后）

| 序 | 片 | 内容 | 交付判据 |
|---|---|---|---|
| 1 | **甲-1 局域网发现（施工单已另开）** | 广播/监听 ＋ **地址分层纯函数** ＋ 「代言」 ＋ 状态行；**不内嵌服务端** | 见[施工单](2026-09-24-lan-discovery-workorder.md) §4 五条（三条 ★） |
| 2 | **B 片 换设备零服务器** | 二维码 / 短码 PAKE 搬**公开材料**（盐 ＋ KDF 参数 ＋ 盒）＋ 设备指纹，不经服务器 | 与甲-1 正交，可独立验收；见[施工单](2026-09-25-b-zero-server-pairing-workorder.md) §4 五条（三条 ★） |
| 3 | ~~甲-2 内嵌接待窗口~~ **⚠️ 2026-09-25 冻结** | 原内容（客户端自己实现接收侧：账本 ＋ 发号牌 ＋ `push`/`pull`）**不发号牌、不留账本的那一半（传输面）改由 §13 的丙-③ 兑现**；「发号牌 ＋ 账本」那一半**作废**（丙 §4 要删的正是这两样） | 施工单 [**已冻结**](2026-09-25-a2-embedded-reception-workorder.md)（单据顶部记着冻结理由、哪一半仍要复用、以及一条已查明的真缺口） |
| 4 | 丙 真网状 | **① HLC 判序 ＋ 仿真夹具：已落（见 §12）** → ② per-peer 水位 ③ wire 带戳 ④ 附件对端选择 ⑤ 裁决 UI 重做 | ★ 就是 §6 丙那一格 —— 夹具已照它建好（§12）；**除第一片外仍是目标态、不排期** |

⚠️ 甲-1 **不自带接待窗口**，所以它单独上线时的价值取决于「网段里有没有在代言的设备」——
这就是把「代言」划进甲-1 的理由（否则它只是一块砖）：见施工单 §0 的实话那一段与 §2 的 ③。

## 11. 丙 上线之后：**使用场景**（用人话，拍板与验收时看这一节）

先说总口径：**台数今天就没有上限**（N 台各自与中枢换增量），所以丙 改的**不是「能接几台」**，
而是把「**必须有一个地方发号牌**」这件事整条删掉。下面每一格的差别，都能追回同一句：
**「中枢在不在」不再是同步能不能发生的条件。**

### 11.1 会变得不一样的（用户真能感觉到的）

| # | 甲档（今天） | 丙 档 | 用户看到什么 |
|---|---|---|---|
| S1 | 网里必须**有一台常开设备**当中枢；它关机 / 合盖 ⇒ 同网段同步**暂停** | 没有「中枢」这个角色 | 两台都合盖再打开，照常接着同步 —— 不用再约定「谁的电脑不关」 |
| S2 | 中枢＝权威，别的设备围着它转 | 中枢**可以留着**，但降级成「一直在的搬运工」 | 家里那台旧笔记本可以继续常开（省电、少握手），它掉线**不再是一条事故** |
| S3 | 号牌由服务端 / 中枢发，一次一个 | 每条记录自带 `(毫秒, 计数器, 设备号)` | 增量**到达顺序不再要求一致**；谁先连上、谁后连上都不影响最终结果 |
| S4 | 离线设备要「连上中枢」才能并增量 | 两台设备**直接**交换增量 | 高铁上改了三天，回来同一个 Wi-Fi 下直接并 |
| S5 | 「同一笔经两条路到达」不会发生 | 会（网状里天然会） | 由**幂等**兜住：同一版再来一次一字不变（不弹重复、不翻倍） |

### 11.2 不会变的（**别读强**）

- **冲突不会消失**。同一段内容两台同时改仍然是冲突。丙 换的是「谁更新」的**判据**
  （`seq` → HLC 戳），**不是**「没有冲突」。而裁决 UI（`dirty`、待取回存档、冲突提示）是
  **为 `seq` 写的** ⇒ 丙 要**重做**它：这是丙 真正的大头（§4 最后一条），**本片没碰**。
- **不承诺**跨网段 / NAT 穿透 / 中继 / Web 参与（§7）。局域网够不着，仍然显式回落。
- **「看上去更新的那版」与「真实时间」会分家**：戳里的毫秒是**设备自己的表**。表被拨到未来的那台，
  它的改动会在很长一段时间里压过别人 —— **收敛（大家一致）不受影响**，但「谁更新」是按戳走的，
  **别把 HLC 当时间审计**。这条边界在判据里专门钉了一格（§12 的「表偏 ±1 小时」）。
- **不是免费的**：每条过网记录多存一列版本戳（可比、可索引的文本），水位从 per-workspace
  推广成 **per-peer**，附件要能回答「跟谁要哪份」。

### 11.3 三个具体到可以照着演的场景

**场景一：两个人的咖啡馆（两台笔记本，网里没有服务器）**
A 与 B 被发现层认出来（甲-1 那块砖）。A 改了第 3 页的标题、B 改了同一页的另一段。
两条线 A→B、B→A 各送自己的增量：到达顺序无所谓（S3）、重复也无所谓（S5）。
两边打开都是「两处改动都在」，而**这个过程里没有任何一台在发号牌**。
A 先走了，B 继续改；第二天在办公室又遇上 ⇒ B 把昨天那段一起给 A。
因为戳跟着记录走，「上一次同步到哪」这个**全局**水位不再是必需品（那一格变成 per-peer）。

**场景二：家里那台旧笔记本还开着（中枢降级）**
这台机器今天在甲档里是权威。丙 里它仍是网里的一员，只是**碰巧一直在**。
它掉线 ⇒ 另外两台照样互相同步（S1/S2）；它回来 ⇒ 收下错过的那几条，凭戳自己算赢家。
★ **本片的判据就是这条**：同一批消息，「直连」与「经中转者」两趟（**连投递顺序都不同**）
算出来的投影**逐字节相同**（§12）。

**场景三：三个人开会（手机 ＋ 平板 ＋ 笔记本）**
三台互相发现、各自发增量。没有「谁先连上」的要求，也没有「谁当主机」的选举。
散会之后每台都是同一份内容。

### 11.4 一句话回答「为什么值得做」

甲档把「不依赖外部服务器」当场兑现了，但它**依赖网里有一台不关机的设备**；
丙 把**那最后一台**也删掉了 —— 代价收敛在「裁决 UI 重做」这一件上（§4）。

## 12. 丙 第一片已落地：**HLC 判序 ＋ 仿真夹具**（2026-09-25）

拍板之后的第一片，**只落纯函数与判据，产品路径一行没动**（与冲刺的「先纯函数、再接线」同形）。
跑法：`powershell -ExecutionPolicy Bypass -File scripts\win-cargo-test.ps1 -Filter hlc::`
（`mesh_sim::` 同理；Rust 全量要在**桌面应用没在跑**的时候跑）。

| 落了什么 | 在哪 | 判据（★ ＝ 承重） |
|---|---|---|
| **HLC 判序** | `src-tauri/src/hlc.rs` | `tick` 单调（物理钟**停住或回拨**也单调，靠计数器兜底）；★ `observe` 回来的戳**一定压过**它刚收到的那枚（因果在序里，本机的表慢一小时也压不住）；全序（反对称 ＋ 传递 ＋ 本层产生的戳**两两不同**，平局由**设备号**分开）；定长编码**字典序 == HLC 序**、且**只认规范形态**（短写当场 `None`，不猜着补齐）；计数器用满 ⇒ 宁可多走一毫秒也**不发重戳**；并库**幂等 ＋ 与到达顺序无关**；墓碑带戳、不进投影、**迟到的旧编辑不许复活** |
| **仿真夹具** | `src-tauri/src/mesh_sim.rs`（`#[cfg(test)]`，**不进产品二进制**） | ★ **决策简报 §6 丙那一格**：四台设备、**没有中枢、没有号牌**（每条戳的作者必须是它自己 —— 这就是「没有号牌」的可检验形态）⇒ 乱序 ＋ 重复投递后**投影逐字节相同**；10 个排列 × 每第 3 条重投，全都不变；★ **经中转者与直连两趟（投递顺序还不同）投影相同**（「去掉中枢进程之后仍然收敛」）；表偏 ±1 小时照样收敛；因果压过慢一小时的钟；**交错编辑**（第 2 轮的改发生在**收到之后** ⇒ `observe` 真的上场，不像前几条那样只验"先编辑完再乱序投递"）也收敛、重投一字不变；**负对照**：把全序换成「只比 `(毫秒, 计数器)`、平局先到者赢」⇒ 同一批消息换个顺序就**分家**（证明上面几条真的咬人）；洗牌本身确定性可复现 |

**本片明确没做**（都留给接线那一片）：

- 没加 wire 字段、没动 `sync.rs` 任何一个分支、**没加 Tauri 命令**、**没动 schema**；
- **没落 per-peer 水位**（「我见过 A 到某时某刻」）—— 夹具里它是隐形的（消息全投），真接线要显式建；
- **没决定「删除 vs 并发编辑」的策略**：现在按**戳大的赢**（＝后删除压过先编辑），要不要改成
  add-wins 是**策略**，见 §4；`hlc.rs` 的「不决定的事」一节把这条写死了，免得后面把它当承诺；
- **没有第二份实现**：本片只落 **Rust 一侧**。丙 需要判序的是**非正文那半边**（整页 14 列 ＋
  删除墓碑 ＋ 附件元数据），它只在桌面（Rust）上被应用。Web 那边是「**产品上不提供**多设备同步」
  （F7：2026-09-15 产品决定，2026-09-25 owner 再确认）——这条的**准确读法**是：不是「没有实现」，
  `web.ts` 那份 TS 同步引擎**完整存在、且按裁定保持**，只是**不跟着演进**（删了平台层不完整）。
  ⇒ 这时写 TS 镜像收益是零（产品上根本打不开），代价是多一份**没有消费方**的实现跟着漂移。
  真要开 Web 同步，那一格应与该开的 UI / CORS 一起做。这与 `block_rev` / `crdt_wire` 那两对
  「两侧成对」的区别就在这里（那两对是**两侧今天都在跑**），如实写清楚，不留白。
- ⚠️ **给接线那一片的前置约束**（从 F7 读出来的，本片已按它写代码）：丙 的戳一旦上 wire，
  **Web 那份"按裁定保持"的实现会读到不认识的字段**。所以戳的读法必须与
  `crdt_wire` / `wireState` 同一条纪律：**不认识就如实报、不猜**（`crdt_wire.rs` 的
  `UnknownVersion` 就是先例），**绝不许"读不出来就当空"** —— 那等于把对端那一版静默丢掉。

⚠️ 一条**收据**：`lib.rs` 里 `mod hlc;` 上面那行 `#[allow(dead_code)]` 是「**还没接线**」的标记
（本片它确实没有调用方）。**接线那一片顺手删掉它** —— 与 `lan.rs` 当初那条是同一纪律。

## 13. 丙 的推进分片（owner 2026-09-25：**直接向丙进化**）

owner 原话：「我希望直接向丙进化」⇒ **甲-2（内嵌接待窗口：客户端自己发号牌 ＋ 留账本）冻结**，
力气改放到丙 上。这不是"丙 提前"，而是「**不再为丙要删掉的那一半投资**」：

- **甲-2 的价值目标保留**：拆掉「得先装个 `shuyonote-sync-server`」这道门槛 ——
  改由下面的 **③ 对等交换面**兑现（丙 同样需要"客户端之间互相收发记录"，只是**没有号牌、没有账本**）。
- **甲-2 作废的是**：`changes.id` 当全局序（发号牌）与「账本 ＝ 那张表」这两件 —— 正是 §4 要删的。
  施工单已按冻结处理，单据顶部记着"哪一半仍要复用"。

⚠️ **代价要说清楚**（这是这次改口径的真实价格）：**「不装服务端也能同步」会推迟**。
甲-2 原本是近期就能兑现的那一步（它只差接线）；改走丙 之后，这件事要等 ③ 落地。
**甲-1（局域网发现）与 B 片（零服务器换设备）不受影响** —— 两片都已落。

| 片 | 内容 | 承重判据（★ ＝ 退化了就变红） |
|---|---|---|
| **①** ✅ | HLC 判序 ＝ 纯函数 ＋ LWW 折叠 ＋ 投影；N 对端仿真夹具（见 §12） | 已落：乱序 ＋ 重复投递后投影逐字节相同；去掉中转者仍然收敛；负对照证明判据咬人 |
| **②** | **戳挂到记录上**：读 / 挂 / 去掉（纯函数；**不认识 ⇒ 如实报、不猜**）＋ **有戳按戳判胜负、没戳走今天那条路**（两条混用时的规则要**写死**） | 同一页两版 ⇒ **与到达顺序无关**地收敛；混用时有明确定义的一支；★ **没戳那条路逐字节等于今天**（老对端零感知） |
| **③-a** ✅ | **戳真的过网 ＋ 页级胜负按戳判**：页 upsert 落 outbox 时挂上**本机戳**（挂载荷里，老对端读不懂就忽略）；收侧解密后 `stamp_of_payload` ⇒ `verdict` ⇒ **两边都有戳才按戳判**，缺一边**原样走今天那条路并留痕**；本机时钟与"本页当前那枚戳"落 KV（**不新开 schema 列**） | ✅ 已落（2026-09-25）：`sync.rs` 5 条 ＋ `doc_content` 的注入点 1 条；**变异实测**：把 `StampWins::Remote` 降级成"今天那条路" ⇒ 判据当场红。★ 反向保护：**既有那一整批**（`doc_content` / `sync`）一条都不带戳 ⇒ "没戳就逐字节等于今天"由它们全体看着 |
| **③-b-1** ✅ | **对等交换的内核**（新 `src-tauri/src/mesh.rs`）：客户端之间**直接**收 / 发**带戳的记录** —— 服务侧**只服务它自己产生的**记录（`WHERE device_id = 我` ⇒ **没有账本**）＋ **每条对等体一个水位**（KV `mesh_cursor:<空间>:<对端>`，值＝发送方自己的 `device_seq`）＋ 只绑内网的守卫 ＋ 对中枢那一套端点**显式 501** | ✅ 已落（2026-09-25）：★★ **两台客户端、没有中枢、没有服务端进程**、TCP 走真环回 ⇒ 互换一笔改动后**两侧投影逐字节相同**；水位只进不退、重复投递幂等；"只服务自己的记录"单独立一条（没有它就得有账本）；**变异实测**：把 apply 跳掉 ⇒ ★★ 判据当场红 |
| **③-b-2a** ✅ | **控制面 ＋ 产品入口**：设置（`mesh_bind:<空间>` ／ `mesh_token:<空间>`，KV，**不新开列**；**"开"只有一个定义＝配了监听地址**）· 窗口的进程级注册表（`ensure_window`，一个空间一个窗口、幂等）· 一轮交换的编排（`round`：**没配 ⇒ 整档关、一个字节都不动**；**一只对端拉不动不连坐**）· 命令 `mesh_sync_now` ＋ 前端三闸（`commands.ts`／`api.ts`／`web.ts` 的**显式"这一档不可用"**） | ✅ 已落（2026-09-25）：★「没配 ⇒ 一个字节都不动」（**变异实测**：不把"没配"当关 ⇒ 判据当场红）· ★★ 编排层真环回拉回一致且幂等 · ★ 掉线对端只在自己那一行报错 · ★ **没有服务端档案也能开网格**（`round` 根本不看 `server_url`） |
| **③-b-2b-1** ✅ | **让它真的被人找到**：① `mesh::announced_base`（**报不出去**的地址——端口 0／回环／公网——一律**不宣告**）② 发现层那一格：**开了网格的空间也发言**，且 `hub_base` 报的是**我自己的窗口**（今天报的还是配置的服务端）；③ 发现层开关加一支"**只开网格没绑服务端**也要起"（否则那台**既不听也不喊**，而每片单测都照绿）④ **设置面命令** `mesh_set_config` ＋ 前端三闸（`None` ＝ 不动，`""` ＝ 清除；关掉时**立刻松口**；回的是**读数**、**不含口令本身**） | ✅ 已落（2026-09-25）：★ 只开网格的空间**照样发言**且报自己的窗口（**变异实测**：去掉那一支 ⇒ 判据当场红）· ★ 覆盖配置的服务端 · ★ 没开网格时两道口径**逐字节是老样子**（走**同一段代码**）· ★ 不在网段里的窗口地址**不宣告** · ★ 读数必须说清"**别人拉不拉得到**" |
| **③-b-2b-2** ✅ | **界面**（`SyncPanel`）：网格那一块 —— 读数（"别人拉不拉得到"那句人话**直接来自 Rust**）＋ 监听地址 ＋ 口令 ＋ 「立刻交换一轮」＋「关掉网格」；读数走 `lan_status` 新增的 `mesh` 字段（**只读、不开窗**），Web 侧同形回"这一档不可用" | ✅ 已落（2026-09-25）：接线判据 5 条（面板真调了那两个命令 · 门槛是 `space_id` 而**不是** `lanRowBound`（网格不需要服务端地址）· 「关掉」走**清除** `""` 而不是 `null` · 那句人话来自 Rust、面板里**不许**自己判回环/公网 · 契约与 Web 两侧都带 `mesh`）。⚠️ **真机两设备复验没做**（要人手）· 自动轮询也**没做**（今天靠按钮，与甲档"按同步"一致的 UX） |
| **④** | 附件对端选择（内容寻址已经在，缺的只是"跟谁要哪份"） | 附件能按内容从**任意一个**对等体取到；一个都取不到时**如实说** |
| **⑤** | **裁决 UI 重做**（今天那套是为 `seq` 写的：`dirty`、待取回存档、冲突提示） | 冲突提示不再依赖 `seq`；「保留本地 / 采用远端」在新判序下**语义正确** —— **这是丙 真正的大头** |

顺序即依赖：**② 必须先于 ③**（记录不带戳，网状就收敛不了）；⑤ 最后，且**只有它做完，丙 才算能用**。

⚠️ **"不装服务端也能同步"截至 ③-b-2b-2 的状态：链路 ＋ 入口都齐了，只差真机复验。**
判序（③-a）· 交换（③-b-1）· 控制面与命令面（③-b-2a）· 发现层互相找到（③-b-2b-1）· **面板入口（③-b-2b-2）** ——
五块各有判据、都在本机可复现。**剩下的两件**：① **真机两设备复验**（要人手：两台机器各自
填监听地址、开面板点一次「立刻交换一轮」）；② 可选：把交换并进「同步」按钮或按周期自动跑
（今天与甲档一样是手动，**不是缺陷**，是口径）。
⚠️ 还有一件**必须记住的口径**：网格**只服务当前空间那一条档案**（`space_id` 是它用来对暗号的东西）
⇒ 没有任何 `sync_profiles` 行的空间暂时用不上网格。

## 14. 死代码稽查（2026-09-25）—— 顺带查出一处**真缺口**

owner 让"清冗余代码"，于是先做了一件最便宜也最该先做的事：**把编译器的话当真**。
`cargo check --lib` 11 条 ＋ `cargo check --lib --tests` 7 条告警，逐条查完**两边都到 0**。
但查的过程里发现的不是"几个小毛病"，而是**这一整类东西没人管**——`#[allow(dead_code)]` 不产生任何输出，
所以它过期了、挡住了什么，都不会有人看见：

| 现场 | 真身 | 处置 |
|---|---|---|
| `sync.rs::IncomingChange.seq` 挂着豁免 | 它**被生产代码读了 8 处**（`apply_pulled_changes` 那一整段）⇒ 豁免早就过期 | 删豁免（**不是**删字段：字段是活的） |
| `commands.rs::mupdf_compiled()` | body 就是 `cfg!(feature)`；**唯一使用者**是一条 `assert_eq!(cfg!(f), cfg!(f))` 的**空转判据**（恒真、零覆盖） | 函数与那条断言一起删；"编没编"改由 `ensure_mupdf_available()` 的两半真验 |
| `security.rs` 的夹具生成器 | 一次"文档与属性被留在上一个函数下面"的事故 ⇒ 一个生成器**被 libtest 注册两遍**（跑两遍），另一个**彻底不可达** | 属性归位 ＋ 判据 `the_manual_fixture_generators_keep_their_attributes_next_to_them` 钉住 |
| `build.rs::find_gm_marker_deprecated` | 实现早搬进 `gm_patch_probe.rs`（好让判据驱动它），**副本还留着**，靠无名无期的豁免挂着 | 删副本（搬走了还留一份，正是两侧漂移的经典形状） |
| `gm_patch_probe.rs` 8 处逐项豁免 | 该模块**在产品二进制里根本不需要存在**（本 crate 里只有判据用它；`build.rs` 走的是 `include!`，那是**另一次编译**） | `lib.rs` 改成 `#[cfg(test)] mod`，8 处豁免一次删清（只留 1 条**跨编译边界**的结构性收据） |
| `hlc.rs` 的模块级豁免 | 丙-③-b 落地后按文件头那张清单逐条核：夹具族（`StampedRecord`/`winner`/`merge_record`/`projection`/`without_stamp`/两个取值器）**只服务判据与仿真夹具** | 逐个 `#[cfg(test)]`；**模块级豁免撤掉**（**不是**"编进来了但没人用"，而是"产品里根本不存在"） |

★ **稽查查出的真缺口（2026-09-25 当天**已修**）**：**收侧没有 `observe`**。
`sync.rs::apply_pulled_changes` 拿到远端戳之后只做了 `verdict`，**没把本机时钟推过它** ⇒
`hlc.rs` 文件头那条"因果一定在序里"在**对端时钟快**时不成立：本机随后的一次编辑可能拿到
**小于**刚收到那枚戳的新戳，于是"因果上更晚的改动"在 `verdict` 里反而判给远端。
修法（同一轮的第二片，`sync::observe_remote_stamp`）：收侧**先 `observe` 再判胜负**，
与 `local_stamp` 共用同一格 KV（跨重启单调照旧）；承重判据
`sync::tests::absorbing_a_fast_peers_stamp_pushes_the_local_clock_past_it` 造一个"表快一小时的对端"，
要求它**压不住**后续本地编辑 —— **变异实测**：把那一次 `observe` 去掉 ⇒ 当场红
（实测读数：本机时钟停在 `0:0:A`，对端那枚是 `4600000:0:FAST`）。

**防复发**：纪律不能只写在文档里（会漂），所以加了门禁 `check-dead-code-receipts` ——
每处 `#[allow(dead_code)]` 必须**删掉 / 门进 `#[cfg(test)]` / 带一句 `// ★ YYYY-MM-DD 收据：为什么留 ＋ 什么时候删`**。
它**不判**理由好不好（那要人读），只判"有没有人会想起来它"；命中必须**在代码里**
（仓里有十几处注释**在讲**这件事，grep 式扫描会把它们全算成违规——自测里有这一格），
生成物（`capabilities_gen.rs`）显式列进 `EXEMPT` 并每次打印。

## 15. 真机复验（丙-③-b，2026-09-26）：**协议全通、数据面为空** —— 抓到并修掉一处 id 混用

owner 让"两台手机测丙的网格"。这一轮把链路真的跑到了真机上，结论分两半：

**通的**（都是真机读数）：两台手机装上带网格的新包（旧包里**根本没有** `mesh_sync_now`，见 §16 的读数）；
互相发现（`本网段发现 1 台`，而且 Mate 40 **自动把同步地址切成了 MIX 2 的网格窗口** —— ③-b-2b-1
"只开网格也发言、报的是自己的窗口"在真机上成立）；两台窗口都起来了（PC 直连两次都通）；
同一个空间、同一个口令（空间不同会回 403、口令不对会 401 —— 两种拒绝都由窗口自己说清）。

**不通的**：MIX 2 上新建一页（走界面同一个命令）后，Mate 40 点「立刻交换一轮」⇒
`fetched 0 / applied 0`，那一页过不去；从 PC 直接问两台窗口也都是 `records: 0`。

**根因（在 `mesh.rs::ensure_window` 里）**：窗口开库用的是

```rust
let conn = crate::db::open_space_conn(space_id)?;   // 传进来的却是**远端**空间 id
```

而库文件名用的是**本地空间 id**（`spaces/<本地 id>.db`，`command.rs` 也是拿 `workspace_id` 去
`open_space_conn`）。真机上两者**不同名**（本地 `default` / 远端 `8be69ab5…`）⇒ 窗口对着一个
**按远端 id 新建的空库**在服务：HTTP 照常 200、`records: []`、**一个错都不报**。

**为什么原判据抓不到**：§13 那条 ★★（两台客户端走真回环、互换一笔改动、投影逐字节相同）是
**直接把已经打开的连接**交给窗口，协议 id 与库名在测试里**恰好是同一个字符串**（`space-x`）——
两个命名空间重合，怎么写都绿。**真机是唯一会把它们分开的地方**，这正是"必须做真机复验"的实证。

**修法**（两个 id 各司其职，不再混用）：`ensure_window(db_space, proto_space, device_id, cfg)`
—— `db_space` 开库、`proto_space` 进 `MeshConfig`（403 检查 / 对端匹配）；`mesh_scope` 改成带回
`MeshScope { space, db_space, device }`；发现层循环（`lan_state.rs`）同样把 `ws_id` 喂给开库那一格。

**判据（两层，含变异实测）**：
1. `mesh::tests::the_window_serves_the_local_spaces_db_not_a_file_named_after_the_remote_id`
   —— **真库文件**上造"两个 id 不同名"的现场（本地 `default` ＋ 远端 `space-x`），要求窗口把那一条
   服务出来、并且问错 id 必须 403。**变异实测**：把开库那一格换回远端 id ⇒ 当场红，
   报的正是真机那句症状：`窗口服务的是空库…：{"records":[]}`；
2. `sync::tests::the_two_mesh_space_ids_are_wired_to_their_own_purposes`（源码级）
   —— 钉**调用点**把两个 id 喂对（`mesh_sync_now` / `mesh_set_config` 是 `#[tauri::command]`，
   造 `State<Db>` 现场会踩"单跑红、全量绿"那一族，所以这两处用源码级）。

### 15.1 修完之后：★ **在真机上闭合了**

同一套步骤（两台手机、同一 Wi‑Fi、**关掉自动同步** ⇒ 不借服务端那条路），Mate 40 点一次
「立刻交换一轮」：

| 读数 | 改前 | 改后 |
|---|---|---|
| `fetched / applied` | `0 / 0`，`error: null`（**静默**） | **`40 / 40`**，`error: null` |
| Mate 40 的页数 | 32（不动） | 0 → **34** |
| 改前过不去的那一页 | 不在 | **在**（`f59db15b…`，就是 MIX 2 上新建的那一条） |

⇒ **"去掉中枢仍然收敛"在真机上成立**：Mate 40 拉的是 MIX 2 的网格窗口
（`http://192.168.43.96:47832`），服务端那条路是关着的。

⚠️ 中途撞到一次**配置**问题（不是代码缺陷，但值得写下来）：两台原先挂的是
**不同加密口径**的两个空间（MIX 2 明文 / Mate 40 个人空间加密），只是**碰巧绑了同一个组织空间 id**
⇒ 明文载荷对不上密文钥匙，收侧如实报 `同步解密失败：Invalid symbol 123, offset 0` 并**停下**
（没有静默丢数据）。⇒ 口径：**网格两端必须是同一个"加密空间"**（同一把空间钥匙）。
这不是网格引入的约束 —— 甲档那条路同样要求它，只是甲档"先经服务器"让人不容易看出来。

### 15.2 同一轮里修掉的界面问题：网格那一块在窄屏被挤成一团

owner 截图 + 真机 CDP 实测的读数（`display:flex; flex-direction:row`，318px 宽里）：
**输入框只剩 50px**（屏幕上就是 `192.`）、**按钮 44~50px 宽 × 64~112px 高**（「保存地址」折成
"保/存/地/址"）。根因是**复用了错的基础类**：`.sync-att` 是"说明文字 ＋ 一个复选框"的横排类，
而网格那一块是"读数 ＋ 两行『输入＋按钮』＋ 一行按钮"的竖排。修法见 CHANGELOG；
判据两头：**真机量几何**（改前/改后都有读数）＋ 文本级 CSS 形状判据
（`verify-mobile-overlays.mjs` **量不到**它：那一块在 `isDesktopPlatform()` 后面，而那条门禁跑 Web 平台）。

