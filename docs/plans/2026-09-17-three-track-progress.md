# 三线整体进展（国密 / PDF / AI）· 2026-09-17

> 状态：**进度口径**（不是方案，不改任何设计）。用途：让 Windows／AMD／Mac 三方看到**同一份现状**，
> 少一次"我这边以为在等它、它以为在等我"。
> 三条线的方案分别在：[国密全链路](2026-09-16-sm-crypto-full-plan.md)、[PDFium 替换](2026-09-16-pdfium-engine-plan.md)、
> [国密 vs 原方案利弊](2026-09-17-sm-crypto-tradeoff.md)。
> **口径截止：`dev = 53dc2f2`、`main = c907a1c`**（GitCode 与 GitHub 一致）。

---

## 0. 一句话

**AI 走得最快**（当天提交几乎全是它，还立了"会咬人的"红线门禁）；**PDF 是"准备与验证做完、接线没做"**；
**国密是"决策全定、但整线卡在 P0 上没动"。**

---

## 1. 国密（档 3 全链路）

| 阶段 | 状态 | 依据 |
|---|---|---|
| **决策** | ✅ **全定**：8 项拍板 ＋ §0.1 套件常量表 ＋ 三边分工 | §0 / §0.1 / §9.1 |
| **准备（真证据，不是承诺）** | ✅ Tongsuo 构建：**Linux 13 秒**、**Android arm64 9 秒**（历史那道坎过了）；✅ **对拍夹具 8/8 PASS**（RustCrypto↔Tongsuo 双向、GM/T 0002/0004 标准向量、**两侧密文逐字节相同**）；✅ RustCrypto 四件套可用性实测（`sm4 0.6.0`/`cbc 0.2.1`/`sm3 0.5.0`/`hmac 0.13.0`） | §3 Tongsuo 情报、§4 第 5 条 |
| **实现** | ❌ **P0 密文版本化未开工**（`crypto.rs` 无版本头）⇒ **整线卡在这里**；P1/P2/P3 均未开工 | 本次核 `origin/dev` 的 `crypto.rs` 与 `Cargo.toml` |
| **排期变更（2026-09-17）** | P2/P3 **已合并为一条 provider 补丁线**（SM3 先、SM4 后，同一文件同一结构体） | §3.1 |
| **卡点** | **P0 是硬前置**（KDF/HMAC 一换，旧库连页 HMAC 都验不过＝**直接打不开**），负责人 **Mac**，尚未动 | §3.1 第 2 条 |
| **Windows 欠账** | **Tongsuo 的 MSVC 构建没做**（我认领的空档任务，也是 P3 第一关） | §5.2 第 3 条 |

## 2. PDF

| 线 | 状态 |
|---|---|
| **PDFium 替换** | P0 ✅（库＋**四平台校验和**＋**attestation 权威哈希交叉核对**＋`SOURCE.txt` 溯源）；P1 ✅（`pdfium_native.rs` 编译通过、**Linux 交叉验证 ok**、AMD **8 条复核已逐条处置**：5 改／2 转 P2 验收项／1 已对齐） |
| | ✅ **P2 运行时开关分派已落地**（Windows，分支 `feat/pdfium-engine @ 6caa4e0`；`SHUYONOTE_PDF_ENGINE=pdfium` 时走 PDFium，**缺省仍是 MuPDF**）—— **待 Linux/macOS 交叉验证**（`cargo check --lib`，须核 HEAD）；❌ **P3 对拍未做**（方案原话「**不做对拍不算完成**」）；❌ P4 多平台打包未做（但校验和这关已清） |
| **M24 批注/阅读器/OCR** | ✅ 历史早已落地（阶段 1＋3、连续滚动、护眼、本地 OCR、AI 一键目录、朗读） |
| **卡点** | **P3 对拍**（方案原话「不做对拍不算完成」；两引擎已可切换 ⇒ 前提具备）；另等 **Mac 跑一次 `cargo check`**（新加的 `#[cfg(target_os="macos")]` 分支 Windows 证明不了） |

## 3. AI

| 线 | 状态 |
|---|---|
| 产品历史能力 | ✅ 薄 Agent、内联起草、AI 帮读 / 整篇 PDF 提问、OCR/视觉识别、公式识别——均已落地 |
| **AI 全库覆盖 / 抽取**（并发会话主战场） | ✅ `files.read` 落地（**八层全包**，含新 Rust reader）；✅ 桌面库补**派生文本层三张表**（否则桌面上永远是"空"）；✅ `search_chunks` / `read_attachment_text` **Web 侧行为级覆盖**；✅ 「线上判据」登记进外部套件表；✅ docx 段内换行修复、emoji 截断改**按码点**；✅ **红线门禁：抽取层不得 import 任何"能出网"的 ai 模块**（做成会咬人的判据） |
| **内网 Qwen** | 契约已摸清（**只要 protocol / baseUrl / model**，因为 `llm.ts` 只支持 Ollama 或 OpenAI 兼容两种 transport）；能力已确认（**26 万原生 / YaRN 100 万 / 有视觉 / 1080p ≈ 2040 token**）；⚠️ 已发现 `num_ctx` **写死 8192**（协议选择决定要不要改代码）；探测脚本 `tmp/fixture/lan-llm-check.mjs` 已备好 |
| **卡点** | 等 **AMD 的三个值**；若走 Ollama 需先做 `numCtx` 可配 |

---

## 4. 整体看（三条线合起来）

| 视角 | 观察 |
|---|---|
| **瓶颈都不在同一处** | 国密卡 **Mac 的 P0**；PDF 卡 **Windows 的 P2**（最该马上做）；AI 卡 **AMD 的三个值** |
| **公共资产已成形** | 门禁体系（MR #1 **已合入 dev**：清单同源 / 结果公开 / 断言数基线 / CHANGELOG 数字门禁 / GitCode 规则门禁）· 对拍夹具（国密）· 校验和＋溯源＋attestation 核对（PDFium）——三条线都在吃这套 |
| **当天反复出现的元问题** | **基点陷阱 3 次**（AMD 跟踪引用陈旧 / Mac 读旧 `release.yml` / Windows 推陈旧本地分支并据此误报"网络不通"）⇒ 已固化进 `development.md` **§10.5**；**验证纪律**：Windows 跑不了 `cargo test`（`0xC0000139`）⇒ 交叉验证必须**写明被验 commit** |
| **风险排序** | ① **P0 未开工**却是国密全线硬前置；② **PDFium 对拍未做**；③ macOS 库级（CommonCrypto，第二波）；④ 内网模型接线（低） |

## 5. 建议的下一步（按性价比）

1. **Windows 做 PDFium P3 对拍**（P2 已落地，两引擎可切换）—— 样本集按 §0.2-H：生成脚本 ＋ 少量产物入库；
2. **催 P0**（卡着国密整条线；不催只能等）；
3. **Windows 补 Tongsuo 的 MSVC 构建**（自己认领的、也是 P3 第一关）。
