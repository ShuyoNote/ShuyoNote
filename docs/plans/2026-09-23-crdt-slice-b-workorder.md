# Slice B 施工单：让**保存/加载**能选走 ydoc（阶段 2 · 第二切片，2026-09-23）

> 上位：[阶段 2 开工清单](2026-09-23-crdt-stage2-kickoff.md)（Slice A 已落地：`src/lib/crdt/yDocBridge.ts` ＋ 8 条判据）；
> 路线与改动面：[全量 CRDT 冲刺计划](2026-09-18-crdt-full-migration-plan.md) §4；
> 尖刺边界：[CRDT 尖刺结论](2026-09-19-crdt-spike-conclusions.md)。
> ⚠️ 本片**仍不碰服务端、不碰 wire 协议、不碰插件契约**（那三样是 §4 面 2/5/6，属后面的片）。

## 0. 一句话

Slice A 只证明了"JSON ⇄ ydoc 转换是保真的"；Slice B 要回答的是**这条路怎么接到产品上而不同时改变两件事**：
把"选走 ydoc"做成一个**可关的开关**，关掉时行为与今天**逐字节等价**。

## 0.5 已拍板（2026-09-23，owner 回「按你的建议执行」）

§1 那三条口径按下面执行，**不再讨论**（有异议时改这一节并写清理由）：

1. **混版本降级** ⇒ **(a) 落盘仍是 JSON、ydoc 只当内存/传输平面**。理由：与阶段 1 已经跑通的
   "内存/CRDT 用新形态、落盘/同步用老形态"是同一手法；`yDocBridge` 两侧收发的本来就是落盘形态，
   不需要新协议字段，也不需要老客户端做任何事。
2. **空页那段的 id** ⇒ **保存路径先铸，转换层继续不造身份**（Slice A 已为此在缺 id 时当场报错）。
   转换层里 `empty-page` 这个稳定 id 只服务"**还没有任何人铸过**的空页"，一旦保存路径铸过就以它为准。
3. **派生索引**（`content_text` / FTS）⇒ 阶段 1 的"待重建标记 ＋ 补算器"（写层 §11）是**过渡量**；
   ydoc 平面接进来之后，`content_text` 的产出点改由"落盘形态写回之后"统一触发（今天就是这条），
   等真到"合并后重建"那一步再换 —— **本片不换口径，只补判据**。

## 0.6 这个开关**什么时候允许打开**（2026-09-23 追加；方向 = "把它做成真正可用的设置项"）

开关的**唯一合法入口**是**显式 opt-in**：今天是构建期 `VITE_CRDT_PLANE=1`，将来是设置项里的一项。
**不许**出现"按平台／按空间类型／按是否有远端／按时间"之类的**隐式**打开（那类判断会把
"关掉时逐字节不变"这条证据链直接作废 —— 因为没人知道某一台机器上它到底开着没有）。

**发布版一律不开**：`pnpm run build` 的产物里 `VITE_CRDT_PLANE` 是空的 ⇒ 开关恒关；
`plane.test.ts` ③ 已经把"默认不许悄悄打开"钉成判据。

**要谈"让用户能开"，得先满足下面三条**（缺一条都不行）：

| # | 前提 | 现状 |
|---|---|---|
| 1 | Slice B 的 5 条判据全绿（含关掉 → 逐字节等价、切换开关不改已落盘内容） | ✅ **①②③④⑤ 全落地**（`src/lib/crdt/plane.path.test.ts`，9 条；进度见 §3）。⑤ 的口径有一处**如实降级**：它证的是"落后**有痕** ＋ 能收口"，**不是**"开着时正文列永不落后"（理由写在 §3 ⑤ 那段） |
| 2 | 阶段 1 遗留的**真机双设备验收**过 | ❌ 要人手 |
| 3 | **混版本降级**在真机上验过（老客户端写 JSON、新客户端写 ydoc 的那条路） | ❌ 未做（口径已拍：落盘仍是 JSON，所以这一条本质是"老客户端读到的仍是它认识的 JSON"） |
| 4 | **全库块身份已补种**（每个顶层块都有非空 `blockId`） | ❌ 未做 —— **2026-09-23 补 ④⑤ 时实测撞出来的，写在这里免得当成边角情况**：平面开着时 `yDocBridge` 按纪律"缺 id ⇒ 当场报错、不铸身份"，而 `readContent` **与** `readAllContents`（**全库扫描** —— 反链 / `resolve_block` 走它）**都要过平面** ⇒ 库里只要**一页**没补种身份，"开着"就炸在**读**上。判据 `plane.path.test.ts` ⑥ 把这条钉住（关着零感知／开着如实抛错） |

**⚠️ 前提 4 是否成立，取决于另一条**还没拍**的决策**：平面到底住在**磁盘边界**（今天，`docContent` 两读一写）
还是 **wire / 服务端边界**。磁盘边界上**只有一个版本可过** ⇒ 平面**合并不了任何东西**（开着的净效果只是把已落盘
JSON 归一化一次），而且**桌面 Rust 侧根本没接**（那一层的唯一生产 importer 是 `platform/web.ts`）⇒ 开关只改 Web、
安装包一行不变。四个选项、各自的改动面/判据/成本，以及"**只有『每页持久化 ydoc 状态』那条才需要补种块身份**"
都写在 [CRDT 平面住在哪条边界](2026-09-23-crdt-plane-boundary-decision.md)。⇒ **先拍边界，再决定要不要做这份数据迁移。**

> ✅ **2026-09-23 已拍：服务端合并。** ⇒ **前提 4（全库补种块身份）从本表划掉**（它只服务"每页持久化 ydoc 状态"那条路）；
> 前提 3 换成"**wire 载荷要带形态版本标记**，老客户端收到不认识的形态要能如实降级"；
> 并且 Slice B 接在**磁盘边界**的那三处（`docContent.ts:58/83/100`）**应当撤出** —— 撤出是一次代码改动，
> 要单独一次提交 ＋ 把 `plane.path.test.ts` 的 ①②③④⑤ 按新边界**重写**（⑥ 随前提 4 作废），别删判据不写替代。
> 细节与连带项见[决策简报 §6](2026-09-23-crdt-plane-boundary-decision.md)。

**打开之后允许／不允许什么**（写清楚，免得接线时各自理解）：

- **允许**：落盘 `content_json` 的**形态**变化（按 §0.5 第 1 条，这是"过一遍平面"的预期结果）；
- **不允许**：块身份（`blockId`）变化或丢失、`topLevelBlockIds` 集合变化、派生结果（`content_text` / FTS）
  落后于正文 —— 这三样各要有判据；
- **必须能收敛**：关回去之后，同一份内容反复读写**不再改变字节**（判据 ③ 已证这一条）。

## 1. 必须先拍的三条口径（本片存在的理由；拍不出结论就不许写代码）

| # | 问题 | 候选 | 倾向与理由 |
|---|---|---|---|
| 1 | **混版本降级**：老客户端只写 JSON、新客户端写 ydoc ⇒ 同一页两套形态 | (a) 落盘**仍是 JSON**，ydoc 只作**内存/传输**平面；(b) 落盘换 ydoc ＋ 老客户端只读降级；(c) 双写 ＋ 版本标记 | 倾向 **(a)**：与阶段 1 已跑通的"内存/CRDT 用新形态、落盘/同步用老形态"是**同一手法**，风险面最小；`yDocBridge` 已经就是为这条路写的（两侧都是落盘形态） |
| 2 | **空页那一段的 id 由谁铸** | 保存路径先铸／转换层固定稳定 id（Slice A 现在用 `empty-page`）／空页不落身份 | 倾向 **保存路径先铸**、转换层**继续不造身份**（Slice A 已经为此在缺 id 时**当场报错**）；两设备各铸一套 id ⇒ 同一块变两块 |
| 3 | **派生索引（`content_text` / FTS）在 ydoc 平面下何时重建** | 保存时（今天）／合并后（CRDT 时代） | 阶段 1 的"待重建标记 ＋ 补算器"（写层 §11）是**过渡量**：换 ydoc 后产出点变成"合并后" ⇒ 补算器口径要跟着改，本片至少要把**判据**先立起来 |

## 2. 交付面（只在 §4 面 1 里动，别扩）

1. **开关**：一个默认关闭的选项（名字待定，不进 UI 也行 —— 先供判据与埋点用），关闭时**不改变任何落盘字节**；
2. **接入点**：`src/lib/docContent.ts` 那一层（"只经一层"的门禁 `check-doc-content-access` 已经在看着这里）；
3. **往返不变量**：开关打开时，`读 → 合并 → 写` 全链路仍以**落盘形态**出入（ydoc 只在中间当平面），
   ⇒ 关掉开关得到与今天**逐字节相同**的结果。

## 3. 判据（Node 侧自验 ⇒ Windows 也能跑）

**进度（2026-09-23，同日两轮）**：

1. **第一轮**（`a8177952` / `b49c9ed2` / `ded2ca91`）：开关骨架 ＋ 接进 `docContent` 的**两条出入口**
   （读/写各一处）＋ 路径级 ① ② ③；期间撞出**模块初始化环**，由 `52bf9010` 用"平面实现改成**注入**"断掉
   （那一层不再把整张编辑器节点表拖进依赖图）。
2. **第二轮**（本轮）：补齐 ④ 引用完整性、⑤ 派生口径，另补一条**实测撞出来的** ⑥ 存量风险。读数：
   `src/lib/crdt/` **3 文件 / 20 条全绿**（其中路径级 9 条）、`check-doc-content-access` **562/562**（基线未动）、
   `tsc` exit=0、`pnpm run build` exit=0。

⚠️ **⑤ 的口径如实降级（不许写成"开着时正文列也对"）**：合并产物是**拼出来**的，它的产出点（磁盘写入 /
远端应用）与派生正文的产出点（"有编辑器的那一侧按编辑器语义算一遍"）**本来就不是同一处**
（`mergeRemoteContent` 的长注释里那条已知边界）。本片对 ⑤ 的验收口径因此是两件事，都写成了判据：
**(a)** 平面**只碰内容形态**、不碰派生列（两条模式下 `content_text` 逐字节相同）；
**(b)** 正文列**真的落后**时**有痕**（`text_stale` 打上、进补算队列）且补算器能把它收口。
"有痕 ＋ 能收口"就是"不静默落后"的可验证形态 —— 与裁定 (iii)"不许静默选边"同一精神。

**接线步骤（下一轮照着做，别扩面）**：

1. 在 `src/lib/docContent.ts` 的**两条出入口**上各包一次 `throughCrdtPlane`：写入前（`content_json` 将要落盘那一步）
   与读取后（远端/本地内容进入内存模型之前）；**只在这两处**，别撒到调用方 —— "只经一层"的门禁
   `check-doc-content-access` 正看着这里；
2. 把 `plane.test.ts` 的 ① 升级成**路径级**：同一批 fixtures 在开关关/开两种模式下，比较
   `write` / `applyRemoteContent` 之后落库的 `content_json` —— 关着必须**逐字节相同**，
   按 §0.5 第 1 条"老客户端零感知"，开着则允许不同（但必须内容等价）；
3. 加一条"**切换开关不改变已落盘内容**"：先关着写一版、再开着读一遍写回，落库字节不变。

| # | 判据 | 为什么它承重 |
|---|---|---|
| ① | **开关关闭 ⇒ 逐字节等价**：同一批 fixtures 在开/关两种模式下写出的 `content_json` 完全相同 | 迁移不许改未开启用户的行为 —— 这条不绿，后面全是空谈 |
| ② | 开关打开 ⇒ 走 `contentJsonToYDoc` / `yDocToContentJson` 往返后**内容等价**（用 Slice A 那套归一后的比较口径） | 接线接对了没有 |
| ③ | **混版本降级路径**：按 §1 拍板的口径造一例"老形态输入 ＋ 新形态输出"，断言不丢块、不静默改写 | 尖刺 §6 点名要求 |
| ④ ✅ | 引用完整性：往返后 `topLevelBlockIds` **集合与顺序**不变（块引用/反链不会断） | 这是本产品与"普通 Markdown 编辑器"的差别所在。判据刻意用**产品自己那个读块身份的函数**（`blockIdentity::topLevelBlockIds` —— `Editor.tsx` 的锚点与反链都从它出发），不是自己再数一遍 JSON（那证明的是"我的检查器"，不是"产品读得到"）；另附一条"批量出口不丢块"，并断言**开着时字节确实变了**（否则判据恒真） |
| ⑤ ✅ | 派生口径：`content_text` 与 FTS 在两种模式下**都不落后于正文**（或**如实标记"待重建"**） | 接 §1 第 3 条。落地口径见上面那段：**(a)** 平面不碰派生列 ＋ **(b)** 落后**有痕**（`text_stale` ／补算队列）且补算器能收口。⚠️ 括号里那半句"**或如实标记**"就是今天选的，不是可选装饰 |
| ⑥ ➕ | （**第 6 条，超出原定 5 条**）存量页面顶层块没补种身份 ⇒ 开着**读**会如实抛错 | 补 ④⑤ 时实测撞出来的，不是预想的；见 §0.6 **前提 4** —— 它给"开关成为设置项"多堵了一条前置条件 |

## 4. 退出条件

1. §1 三条口径**有明确结论**（写进本文件，不是口头）；
2. 上面 5 条判据**全绿**（＋补的那条 ⑥），且 ① 是"逐字节"级别的；
3. `pnpm run build`（全套门禁）在**当轮 tip** 上绿（阶段 1 与 Slice A 都栽过"只在旧 tip 上跑过"这一条）；
4. 文档索引与 `check-doc-links` 绿。

## 5. 本片**不做**

- 不改服务端、不改 wire/`outbox` 协议、不改插件与 AI 的读写契约（§4 面 2/5/6）；
- 不动版本历史与 undo（§4 面 4）；
- 不把 `yjs` / `@lexical/yjs` 从 devDependencies 升格（升格要单独一次提交 ＋ 说明它对打包产物的影响）。
