# 一键发布到社区：客户端侧方案（ShuyoNote 侧）

> 社区侧已就绪：`shuyo-community@0.71.20`（2026-09-20 上线），接口契约见该仓库 `docs/api.md` **§7**。
> 这一份只讲**这一侧**要做什么。分支：`feat/publish-to-community`（从 `origin/dev` 开出）。

## 0. 与既有方案的关系（先对账，别冲突）

**本文档不是新方案，是既有 P0 第 3 条的落地。** 既有两份：

- [`plans/2026-09-11-app-community-interactions.md`](2026-09-11-app-community-interactions.md)
  —— §三 优先级表第 3 条「应用 → 分享笔记到社区（摘要为主）」，阶段 **P0**，依赖写的是「社区发帖 API」；
- [`community-integration-status.md`](../community-integration-status.md) §五 对账表第 3 行：
  **❌ 没做**，卡在「依赖社区发帖 API（现在没有）」＋「要用户自己的凭据」，
  并明确标注这是「**要用户拍板**的一类，不该由 agent 自行决定」。

对账结果（三条，逐条落实）：

| 既有方案说的 | 现在怎样 |
|---|---|
| 依赖「社区发帖 API」（当时没有） | **依赖已解除**：社区 0.71.20 起有 `POST /api/posts` ＋ `Idempotency-Key` ＋ `source` 标记 |
| 「要用户自己的凭据（发帖代表他本人）」 | **换成设备码应用令牌**：用户在网页上确认一次，客户端**不存密码**、令牌 180 天且可撤销、只能发帖 |
| §一 底线：**任何"自动上传"默认关闭；上传前必须给出清单，并由人手动确认** | **本文档补上了这一条**（I7 ＋ P0 判据⑦）——既有文档里那条「分享默认只带摘要 + 原帖链接，全文要不要发由人点」的**前半句来自 URL 长度限制**（`compose` 走 `shuyonote://`，约 2000 字符），而这条通道是 **API 不是 URL**，长度不适用；**后半句「由人点」照旧成立** |

**已决（owner，2026-09-20）**：发出去的是**整篇正文**，靠「**发布前清单 + 人确认**」守底线（I7），
不做默认截断。既有方案里「摘要为主」那半句原本是为绕开 `compose` 的 URL 长度上限（约 2000 字符），
本通道是 API 不是 URL，该约束不适用；「全文要不要发由人点」照旧成立。
**若日后要改回摘要模式**：改的是 P0 的清单/组装，后端通道不用动。

**另有一处接口交汇，先记下**：既有方案 P1「同一身份（我的帖子 / 我的插件）」写的是**复用登录令牌**，
而这里用的是设备码令牌，scope 只有 `post:create post:update` ⇒ **读不了 `/api/me/posts`**
（它不在社区侧应用令牌白名单里）。到 P1 那一步要么给社区侧加一条（`post:read`），要么另想 —— 别到那时才发现。

## 0. 一句话

让 ShuyoNote 能把**一篇笔记**发到社区，并且知道「发过没有、发的是哪一版、线上是不是被人改过」。

## 1. 社区侧已经给了什么（照契约写，不必读它的 Rust）

| 社区能力 | 客户端要对应做的事 |
|---|---|
| `source`（白名单 `shuyonote`）/ `source_ref` | 发帖时带 `source="shuyonote"`、`source_ref=<笔记 id>:<修订>` |
| `Idempotency-Key`（同 key 回放首次响应） | 一篇笔记一个**稳定** key：同修订重发永远只落一篇 |
| 设备码授权（`/device` ＋ `/api/auth/app/*`） | 「连接社区」：**不收用户密码**，换一把 180 天、可撤销、只能发帖的令牌 |
| `POST /api/attachments`（魔数白名单，内容寻址） | 本地图片先上传换成 `/attachments/<hash>` 再发 |
| `GET /api/posts/{id}/versions` | 「线上是不是被人改过」的判据来源 |
| scope 白名单（其余一律 403 `app_token_scope`） | 令牌只能发帖/改帖/传图/看自己；点赞评论一律不做 |

## 2. 四件（与社区侧三件一一对应）

1. **连接**：设备码 → 用户在 `/device` 确认 → 换令牌 → 存本地。
2. **组装**：笔记 → `NewPost`（标题、正文 Markdown、tags、图片附件），并算出幂等键。
3. **发送**：`GET /` 取 CSRF cookie → `POST /api/attachments`（图片）→ `POST /api/posts`。
4. **回写**：把返回的 `slug`/`id` + 已发布修订写进笔记的 **front matter**（正文不动）。

## 3. 不变式（写代码前先立，判据围着它们转）

- **I1 笔记是唯一真源**：发布是**单向读取**——社区返回的任何内容都不写回正文，只写发布元数据。
- **I2 同修订重发不产生第二帖**：幂等键必须是 `f(笔记 id, 修订)` 而不是随机数。
- **I3 令牌不进笔记**：令牌只落在 app 数据目录（`app_data_dir`），不进 front matter、不进日志、不进导出。
- **I4 失败要说清是哪一步**：授权 / 图片 / 发帖 / 回写 四步各自报自己的错，不做"反正失败了"的合并。
- **I5 不静默覆盖**：线上正文与本地不一致时**先提示**，由人决定"以本地覆盖"还是"先看线上"。
- **I6 未连接不是错误**：没连接时入口显示"连接社区"，而不是让发布按钮报错。
- **I7 发布前必须给人看清单**（既有方案 §一 的底线条款，这一条是硬约束）：弹窗里要能看见**将要发出去的东西**
  —— 标题、标签、正文（全文还是摘要、多少字）、图片张数 —— **人点确认才发**；默认状态是"没发"，
  没有任何定时、没有"顺手同步"、没有"上次发过就自动再发"。判据不看代码怎么写，看**没点确认时社区侧帖数不变**。

## 4. 发送时的判读（社区返回什么就说什么）

| 响应 | 含义 | 客户端动作 |
|---|---|---|
| `200`（首次 / 同 key 回放） | 成功 | 回写 slug/id；回放的响应与新发**逐字相同**，不必区分 |
| `409 duplicate_in_flight` | 上一个同 key 还在处理 | 稍后重试，**不是**错误提示 |
| `422` | 审核拦下（`{ok:false,error}`） | 原样展示社区给的理由，不要把 422 当网络错误重试 |
| `401` | 令牌失效/被撤销 | 清掉本地令牌，回到"连接社区" |
| `403 app_token_scope` | 撞到 scope 白名单 | 这是客户端 bug（发错接口了），要如实报 |

## 5. 落点

| 文件 | 内容 |
|---|---|
| `src-tauri/src/community_publish.rs`（新，✅ 已落） | 令牌存储、设备码、`GET /` 取 CSRF、发帖、撤销；纯函数带单测 |
| `src/components/CommunityPublishDialog.tsx`（新，⏳） | 连接 / **清单预览（I7）** / 结果三段式（照 `CommunitySaveDialog.tsx` 的写法） |
| `src/lib/platform/commands.ts` ＋ `web.ts`（✅ 已落） | 5 个命令的 `CommandMap` 契约与 Web 侧落地（Web 版如实说不支持） |
| `src-tauri/src/lib.rs`（✅ 已落） | 注册新命令（挨着 `community::fetch_community_*`） |
| 笔记入口（⏳） | 先放详情/编辑器工具条一枚「发布到社区」（P1 再考虑右键菜单） |

> 组装与响应分类**都在 Rust 侧**（`build_payload` / `classify`），前端不再写一份 —— 两侧各写一份，
> 迟早漂成两种口径（"哪些字符算合法键"就是典型）。所以原计划的 `src/lib/communityPublish.ts` 取消了。

## 6. 令牌放哪（一处取舍，先按 A 做）

- **A（P0 采用）**：`app_data_dir/community-auth.json`，权限按平台收紧（Unix 0600）。与附件同目录，是仓库既有做法。
- B（后续）：OS 钥匙串（Windows Credential Manager / macOS Keychain / Linux secret-service）。
  Linux 无 secret-service 时要回退，属**新依赖 + 三平台实测**，不塞进 P0。
- 无论 A/B：**「断开连接」= 调 `POST /api/auth/app/tokens/revoke` 真撤销**，不是只删本地文件。

## 7. 分期与判据

- **P0（进行中）**：连接（设备码）＋ 发布单篇 ＋ 结果回写 front matter ＋ **发布前清单（I7）**。
  - ✅ 后端通道（`c3d9f5af`）：`src-tauri/src/community_publish.rs`（设备码 / 令牌存储 / 发帖 / 真撤销；
    8 条判据含真环回 HTTP）＋ 前端契约层（`CommandMap` 与 `web.ts` 5 个命令）。
  - ✅ 对话框与入口：`CommunityPublishDialog.tsx`（三段式 ＋ **I7 清单**，13 条渲染级判据）、
    入口挂在编辑器工具条「⋯」菜单（只在桌面端显示）。
  - ✅ **跨仓契约的活判据**：`community::tests::live_community_json_is_accepted_by_this_parser`
    （`#[ignore]`，要联网）—— 拿**线上真站点**的 JSON 喂给应用自己的解析器。这是唯一能抓住
    "对面改了字段名/url 拼法而这边测试全绿"的判据；社区侧 0.71.24 就是 `url` 拼成相对路径被这么抓到的。
  - ⏳ 剩下的：本地图片上传（正文里的本机图片现在只在清单里如实提示"社区上显示不出来"）、
    回写发布元数据（见下面 §7.2 —— 之前写成"回写 front matter"是**错的**：这一侧的笔记是库里的页面，
    根本没有 markdown front matter）。

## 7.2 回写发布元数据：写哪儿（下一笔，先定死）

这一侧**没有 front matter** —— 笔记是 SQLCipher 库里的页面（`content_json` + 属性表）。
所以"回写"要选一个落点，两条路：

| 方案 | 做法 | 代价 |
|---|---|---|
| A. 页面属性 | 用现成的 `set_page_prop`，先 `create_attr` 建三个属性 | **污染用户的属性表**：三个"社区…"列会出现在他们的表格视图里。属性是**用户**的字段，不该由功能自己往里塞 |
| B. 旁表（建议） | 新表 `page_community_publish(page_id PK, slug, url, published_rev, published_at)` ＋ 一个 `community_publish_state(page_id)` 只读命令 | 要一条迁移 + 一个命令；但它是**应用派生数据**，与用户的字段分开 —— 而且 P1「线上是否被人改过 / 落后于线上」要用的是同一张表 |

**建议 B。** 落点定了三件事才敢写："已发布（rev X）/ 线上已变 / 落后于线上"靠它显示，
P2「更新已发布帖子」也要靠它知道该 `PUT` 哪一篇（否则每次都新建一篇）。

  判据：① 未连接时入口是"连接社区"；② 连接成功后掉线重进仍是已连接；③ 同修订连发两次只落一篇（社区侧数
  帖数）；④ 正文里的本地图片发完在社区显示为 `/attachments/<hash>`；⑤ 422 的社区理由原样出现；⑥ 断开后
  再发是 401 → 回到未连接；⑦ **发布前有清单（I7），且"只看不发"时社区侧帖数不变**。
  其中③④要**社区侧**配合才能验全（③ 现在只能守到"前端把同一份 `(noteId, rev)` 递下去、且未确认不递"；
  ④ 属于上面那条 ⏳）。⑤⑥⑦ 已在渲染级判据里。

## 7.1 两个已知口子（写在这里，免得下次当新发现）

- **`rev` 取的是 `page.updated_at`，不是真正的内容修订号。** App 里没有独立的 rev 字段，而
  `save_page` 每次都写 `now_ms()` ⇒ "改一个字再改回去"也会换 rev ⇒ **内容没变也会多发一篇**。
  幂等本身成立（同 rev → 同键），但这个口径下"同内容不同散文"会被当成新版本。P1 要做
  "线上是否被人改过 / 落后于线上"时，得先给笔记一个**内容标识**（内容哈希，或 `list_versions` 的 id）。
- **正文用的是页面快照，不是编辑器实时内容。** 编辑器可能比快照新一次 600ms 防抖。这里选快照是**故意的**：
  `rev` 只能取自快照，正文与 rev 必须同源 —— 否则"同一个 rev、两份内容"会让第二次发布被社区当成
  重放而**静默丢掉**（I2 失效）。人在清单里看到的就是发出去的那一份。要发"所见即最新"，
  得先强制 flush 保存再发（P1）。

- **P1**：发布状态可视化（已发布/线上已变/落后于线上）、右键菜单入口、批量发布。
- **P2**：更新已发布帖子（`PUT /api/posts/{id}`）与"从社区拉回来"合流（复用已有 import）。
- **P3**：多篇/专题、发布前预览（复用 `POST /api/markdown/preview`）。

## 8. 明确不做

- 不做"社区 → 笔记"的回流自动覆盖（已有 import 是**人点**的流程，保持）。
- 不做点赞/收藏/评论/私信 —— 令牌的 scope 根本不给。
- 不在 P0 做密钥环、不做多账号。
