# 附件同步「范围与刹车」方案（手机端 × 海量视频空间）

> **依据**：2026-09-15 对 `sync_attachments` 的**代码级取证**（起因是一句追问："手机被设置和
> 海量视频空间同步，结果如何？"）。结论是**当前附件同步的语义 = 把整个空间在设备上做一份完整副本**，
> 而不是"按需取用"；这条语义在桌面上是合理的，**换到手机上同时缺三样东西：体积预算、磁盘余量、网络闸门**。
> **目标**：让"手机 × 大空间"**不会在用户不知情的情况下**把设备填满或被系统杀掉；
> 并让"同步了什么、还剩多少、为什么停下"对用户可见。
> **状态**：方案，**未实施**。执行顺序见 **§4.1 建议的执行批次**：
> **第一批**＝ **B1 口径修正（✅ 已完成 2026-09-15）** / B2 补 `end()`（⚠️ **同时是 P6.1 的前置**）/ **B3 ＝ P2a** 导出改真流式 / B4 先实测重复行
> ——**四件都不改同步语义**；**第二批（Android 上架前）**＝ C1 预算刹车（P3）+ C2 网络闸门（P5）。
> **P2b** 目标明确但**全方案风险最高**（牵动加密格式与存量兼容）⇒ **先真机实测，没实测不动格式**；
> **P4 建议不做**（用"每空间开关"替代）；**P6 已立项**（2026-09-15）⇒ 见
> [`2026-09-15-attachment-on-demand-plan.md`](2026-09-15-attachment-on-demand-plan.md)，**先只做「每空间开关」那一半**。
> **本方案不改变"同步仍会拉附件"这一事实**（那是 P6）。
> **范围**：`src-tauri/src/sync.rs`、`src-tauri/src/security.rs`、`src/lib/platform/web.ts`、
> `src/hooks/useAutoSync.ts`、`src/store/syncStatus.ts`、`src/components/SyncPanel.tsx`、
> `docs/MOBILE.md`、`src/App.css`（注释）；跨仓一处：社区仓 `shuyo-community` 的事实核对表 **#40**。
> ⚠️ **本方案的证据全部来自读代码，未在真机 / 真服务端上跑过大文件同步**——见 §七 未验证清单。
> **执行方式**：按 **§4.1 的批次**做，**不是**按 §五 的档位编号顺序做。**B1 可立刻单独发**（零风险）。

---

## 一、结论（先给判断）

现状是**有意设计**，不是疏忽——`docs/SYNC.md:34` 已经把行为写成文档：
"本地有而远端没有的就上传，远端有而本地没有的就**下载到本地字节库**"。
问题不在"它会下载附件"，而在**它没有任何刹车**，且这条设计**默认了桌面环境**：

| 桌面假设 | 手机上的现实 |
|---|---|
| 盘大（几百 GB 起） | 盘小，且与系统共享 |
| 常插电、常宽带 | 电池 + 计量流量 |
| 内存宽裕 | 单个大文件就能触发 OOM |
| 用户坐在机器前 | 后台自动同步，用户不知情 |

**三条真正会伤到用户的，按严重度排**：

1. **无大小上限、无数量上限、无磁盘余量检查** —— 一直下到写不进去为止。
   填满设备不只是"同步失败"：**同一个分区上还躺着 SQLite 库**，写满会牵连数据库写入。
2. **有四条路径会把整个附件读进内存** —— 但**触发条件不同，而这个差别决定先修哪个**（穷举见 **§2.6（2）**）：
   **导出 / 另存为是"无条件"的**（`attachments.rs:378` 的 `std::fs::read` 与是否加密无关）；
   而**导入、同步上传、同步下载只在"用户开了静态加密"时**才整块读（未加密本来就是流式）。
   ⇒ 两个结论：**默认配置下手机同步大文件不会 OOM，它会安静地把设备填满**；
   而"把传进去的视频再下载出来"这条**任何人都踩得到**，且**不需要同步参与**。
3. **自动同步无网络与体积判断** —— 间隔 5 分钟、启动后 3 秒触发、**无条件调用**，
   移动数据下后台拉视频；而且**桌面/安卓侧完全没有附件进度上报**，用户看不见任何东西。

**外加一个必须排在前面修的地基问题**：`useSyncStatus` 的 `end()` **只在 Web 侧被调用**，
桌面/安卓手动同步后状态永远停在"正在同步…"（§二 #19）。**进度上报要建在这上面，所以先修它。**

---

## 二、现状盘点（全部代码取证）

### 2.1 同步范围：全空间双向全集差集

| # | 事实 | 依据 |
|---|---|---|
| 1 | `sync_attachments` 是**双向全集差集**：拉远端**全部**哈希 → 本地有远端无的**全部上传**、远端有本地无的**全部下载** | `src-tauri/src/sync.rs:1496`、`:1514-1529`、`:1572`、`:1631-1690` |
| 2 | 附件**只在绑定空间后**才同步，所以范围**必然是空间级**（不会误拉全服务器） | `sync.rs:1376`（`sync_now` 跳过未绑定）、`:1420`（`sync_workspace` 直接报错）；Web 同构 `web.ts:2432` |
| 3 | 客户端里那个"legacy 全局附件"分支**实际不可达** | `sync.rs:1508-1512` 的两条路径被 #2 挡住 |
| 4 | 该行为**已被文档化**，属有意设计 | `docs/SYNC.md:34` |
| 5 | Web 侧是**同一套逻辑的第二个实现**（不是 stub） | `web.ts:896` `syncAttachments`、`:2429` `sync_now`、`:2541` `sync_workspace` |

> ⚠️ **范围补充（2026-09-15 补）**：附件同步**不区分"页面附件"与"文件夹里的文件"**——
> 二者是**同一张表**（`db.rs:606-616`）、**同一个命令**（`attachments.rs:508` `import_attachment_files`）、
> **同一条通道**；区别只在 `page_id` 指向谁（详见 §2.6（1））。
> 所以本方案说的"附件" = **整个空间的全部文件，不只是页面里的图片**——
> "手机 × 海量视频"的暴露面因此**比直觉更大**。

### 2.2 缺的三样刹车

| # | 缺什么 | 依据 | 后果 |
|---|---|---|---|
| 6 | **无大小 / 数量 / 磁盘余量判断** —— 循环体里只有"哈希合法性"与"本地已有则跳过" | `sync.rs:1631-1690`（`is_valid_attachment_hash` `:1635`、`local_set.contains` `:1638`）；Web 同构 `web.ts:957+` | 下到 `write_all` 失败为止；同分区上的 SQLite 也会受影响 |
| 7 | **无网络 / 时机闸门** —— 自动同步无条件跑 | `src/hooks/useAutoSync.ts:5`（间隔 5 分钟）、`:37`（启动后 3 秒）、`:26`（`api.syncNow()`）、`:39`；`src/App.tsx:618` **无条件调用** | 计量流量下后台拉视频 |
| 8 | **无"按需取字节"** —— 没有"未下载"这个状态 | 全仓无该概念；`sync.rs:1683-1685` 下载即入表（`page_id = NULL`） | 无法只同步元数据 |
| 9 | **无 Wi-Fi / 网络类型能力** | `src-tauri/src` 全仓搜 `wifi` / `is_connected` / `network_type` **零命中** | P5 需要**新能力**，可行性未评估 |

### 2.3 内存、明文与错误处理

| # | 事实 | 依据 |
|---|---|---|
| 10 | **加密开启时整块进内存**：下载后 `std::fs::read` 读**整个文件** → 加密 → 写回；上传侧同理（未加密才流式） | `sync.rs:1670-1676`；上传 `:1590-1595`（`tokio::fs::read`）vs 未加密的流式 `:1597-1599` |
| 11 | **明文落盘窗口**：先把**明文**写 `.part` → rename 成正式文件 → **之后才**加密覆盖 | `sync.rs:1657-1666` → `:1670-1676`。注释的意图是"保持静态加密一致"，但**实现顺序**导致中间态是明文 |
| 12 | **错误处理不对称**：chunk 读取失败用 `?` **炸掉整轮同步**；而 HTTP 非 2xx 只是 `continue` **静默跳过** | `sync.rs:1660` vs `:1646` |
| 13 | 删除**不回收磁盘**：`attachment delete` 只删表行，字节要等清理命令 | `sync.rs:1324-1325`；`storage.rs:243`（`cleanup_orphan_attachments`）、`:356`（`purge_deleted_workspaces`） |
| 14 | 下载的附件以 `page_id = NULL` 入表，名为 `hash.ext` | `sync.rs:1683-1685` |
| 15 | 下载**顺序执行**（循环内 `await`，无并发） | `sync.rs:1631` |

### 2.4 可见性（这是"用户不知情"的根源）

| # | 事实 | 依据 |
|---|---|---|
| 16 | **附件进度只在 Web 侧上报**：`setProgress({phase:"attachments", attCurrent, attTotal})` | `web.ts:920-926`（上传）、`:962-966`（下载）；`SyncPanel.tsx:99` 注释自己写着"**由同步引擎在 web.ts 上报**" |
| 17 | **Rust 侧完全没有进度上报**：`setProgress` / `sync_status` 在 `src-tauri/src` 零命中 | 与 #16 互为反证 |
| 18 | UI 的进度条与计数**依赖 `attTotal > 0`**，所以桌面/安卓上**永不显示** | `SyncPanel.tsx:795-808` |
| 19 | **`end()` 只在 Web 侧调用**：`SyncPanel.tsx:214` 调了 `begin()`，**全仓无配对 `end()`** | `web.ts:2555,2558` 是仅有的两处；`syncStatus.ts:37-41` |
| 20 | 因此桌面/安卓手动同步后 `syncing` 停在 `true`，面板会一直显示"正在同步…"而不是 `:810` 的结果文本 | `SyncPanel.tsx:790`（按 store 的 `syncing` 分支）、`:220`（完成文案走另一分支） |
| 21 | **但"上报进度"这件事仓库里有现成范式可照抄**（所以 #16–#18 不是缺机制，是没接线）：`workspace_io.rs:36-49` 的 `emit(phase, done, total, bytes, message)` → `workspace-progress`；`attachments.rs:539-549` 用**进度回调**发 `attachment-import-progress`；`backup.rs:88-89` 做节流 | `src-tauri/src/workspace_io.rs`、`attachments.rs`、`backup.rs`；前端订阅方式见 `ai.rs:250` 注释 |
| 22 | ⚠️ **`useSyncStream.ts` 不是桌面可用的进度通道**（别拿它当事例）：它是 **Web 专属**的服务端 SSE 订阅，桌面分支直接 return | `useSyncStream.ts:5`（"this is a no-op there"）、`:15`（`if (isDesktopPlatform()) return;`） |

### 2.5 服务端与文档口径

| # | 事实 | 依据 |
|---|---|---|
| 23 | 服务端请求体上限**默认 2 TB**，`--max-body-mb 0` = 不限 ⇒ **单个大视频不受限** | 私有仓 `shuyonote-sync-server` `src/main.rs:68-70`（注释明写"默认 2TB"）、`:470-473`；空间级路由 `:425-434` |
| 24 | **三处文档/注释把 Web 同步写成"是 stub / 没实现"——与代码相反**（#5 证明它是完整实现） | ①`docs/MOBILE.md:18`"`sync_now`/`sync_workspace` **是 stub**"❌ ②`src/App.css:1221` 注释"Web 版不支持多设备同步"❌ ③社区仓事实核对表 **#40**"同步仅桌面版"❌。⚠️ **注意区分两件事**：「**不提供**」作为**产品口径最终是站得住的**（2026-09-15 裁定：Web 版不开多设备同步）；错的是把它写成"**没有实现**"——那与代码相反，且会诱使下一个人把真实存在的实现当死代码删掉 |
| 25 | 而自家 UI 里那句才是**准的** | `SyncPanel.tsx:502`"建议使用桌面版…**Web 版同步受浏览器环境限制**" |
| 26 | 手机浏览器上配置区被 **CSS** 挡住（`pointer-events: none`），所以现实触发条件苛刻——**但唯一的刹车是 CSS** | `SyncPanel.tsx:523` + `src/App.css:1222-1227` |

---

### 2.6 补充盘点（2026-09-15 补：两类上传路径 + "整块进内存"的真实范围 + 一个待实测的副作用）

> 本节是三问之后的追加取证：「**文件夹上传的文件和页面上传的附件有何区别？**」
> 结论是**几乎没有区别**——而这恰好把 §2.1 的范围和 §五 的施工面（**P2a / P2b**）**都放大了**。

**（1）"页面附件"与"文件夹里的文件"是同一个东西，区别只在挂在谁下面**

| 上传位置 | `page_id` 指向 | 前端入口 |
|---|---|---|
| 页面上传的附件 | **那一页**的 id | `AttachmentPanel.tsx:93` |
| **文件夹里上传的文件** | **那个文件夹的 id**（文件夹本身是 `kind='folder'` 的 **page**） | `FileManagerView.tsx:194`、`PageTree.tsx:127`；`commands.rs:133` |
| 画布图片 / 封面 | **`NULL`** —— UI 语义是「**空间根下的未整理文件**」 | `DrawingEditorModal.tsx:279`、`CoverPicker.tsx:72`；`attachments.rs:612` 注释 |

- **共同**：同一张表（`db.rs:606-616`，只有 `id / page_id / name / hash / mime / size / created_at`）、
  同一个命令、**同一套内容寻址**（字节层同内容只存一份，`attachments.rs:561-564`）、**同一条同步通道**；
  连"搬家"都只是改 `page_id`（`attachments.rs:748-771`）。
- **行不去重**：每次导入都 `INSERT` 一行新 uuid（`attachments.rs:578-584`）⇒
  **同内容挂在 N 处就有 N 行、磁盘上仍只有 1 份字节**（即社区仓核对表 #8 那个 ⚠️ 的由来）。
- **「同名文件历史版本」是前端视图，不是存储特性**：`FileManagerView.tsx:314-324` 按 `name` 分组
  （注释原话 "implicitly content-addressed historical versions"）、`:551` 恢复、`:526` 本地名子串搜索。
  ⇒ 三点要记住：① **页面附件面板没有版本入口**（`AttachmentPanel.tsx` 里搜不到任何 `版本`/`version`）；
  ② 它按 **name** 分组，**改过名的同内容文件不算同一文件、同名的不同文件却算**；
  ③ 附件**不进全文检索**（`search.rs` 无 `attachment`；`db.rs:606-616` 只有两个**普通**索引，**无 FTS、无触发器**）。

**（2）"整块进出内存"的真实站点范围：⚠️ 一条无条件 + 三条随加密**

`encrypt_attachment_bytes` / `decrypt_attachment_bytes` 共 8 处调用（另 6 处在测试里），
其中**会读写整个文件**的是这四条 —— **关键是它们的触发条件并不相同**（初版把这四条写成"同病"，不准确）：

| # | 路径 | 位置 | **触发条件** | 形态 |
|---|---|---|---|---|
| a | **导入**（文件夹 / 页面 / 画布**都走它**） | `attachments.rs:568-575` | **仅当开了加密**（`else if key.is_some()`） | 流式写**明文** tmp → rename → **整文件读进内存** → 加密 → 写回 |
| b | **同步下载** | `sync.rs:1654-1677` | **仅当开了加密**（`:1670` `if let Some(k)`) | 分块落盘后**整块读回**再加密写回 |
| c | **同步上传** | `sync.rs:1590-1599` | **仅当开了加密** | 加密时 `tokio::fs::read` 整块读 → 解密 → 发送；**未加密是流式**（`:1597-1599`） |
| d ✅ **已修（B3）** | **导出 / 另存为** | `attachments.rs:374-386` | 🔴 **无条件**（与是否加密无关） | 旧：`std::fs::read` 整块读 → 解密 → `std::fs::write` 整块写；**现：未加密走 `fs::copy`**（见 §4.1 B3） |

- **最重要的推论**：**静态加密是用户主动开启的**（`security.rs:35` 从 meta.db 读，默认关）⇒
  **默认配置下只有 d 会咬人**；a/b/c 要等用户开了加密才出现。
  ⇒ **d 应当从 P2 里拆出来独立做**（见 §四 的 **P2a**）：它零风险、**不改加密格式**、改完就是真流式。
- **d 为什么"无条件"也整块**：`copy_attachment` 先 `std::fs::read`，再 `decrypt_attachment_bytes(key, &raw)`——
  而 key 为 `None` 时该函数**原样返回**（`security.rs` 里 `encrypt/decrypt(None, ..)` 的测试）⇒
  **未加密时这一读一写纯属白费**，可以直接 `std::fs::copy`。
  这条路正是文件管理器的「下载」（`FileManagerView.tsx:538`）⇒
  **手机上"把传进去的视频下载出来"当场 OOM 的就是它，且不需要同步参与。** ✅ **已修（B3）**。
- ⚠️ **口径收紧（B3 落地时复核）**：d 是**唯一"可修"的**无条件整块读。另有一条
  `attachment_bytes` / `read_attachment_bytes`（`attachments.rs:431`、`:442`）也是无条件整块读，
  但**它不在可修之列**：那条路要把整份字节**通过 IPC 交给 JS**（`read_attachment_bytes` 用
  `tauri::ipc::Response` 回原始字节，正是为了避开 JSON 数字数组），
  "整份同时在内存里"是这条通路的定义，不是浪费 ⇒ **别把它当成 d 的同类去"优化"**。
- a 与 d 是**本地操作**、与同步无关；**只有 b/c 在同步链路上**。
- 另有第 5 类 `attachments.rs:431-439`（`attachment_bytes`）会把**整个附件**解密后交给前端；
  但它是否落在**大媒体预览**的热路径上**本次未核实**（大媒体很可能走 `attachment_path` + 资源协议，
  那样就不进内存）⇒ **要动它之前先确认这一点**。

**（3）一个代码推断的副作用：第二台设备会多出重复行 —— ✅ 已判定成立并已修（2026-09-15，B4）**

- 经 `changes` 进来的附件行带**正确的 `page_id`**（`sync.rs` 的 `do_pull` 附件 upsert）；
- 字节下载路径**又插一行** `page_id = NULL`（**新 uuid** + `INSERT OR IGNORE`，
  而 `INSERT OR IGNORE` 只对**主键** `id` 冲突生效 ⇒ **永不冲突**）⇒ 同一 hash **两行、两个 id**。
- ⇒ 在第二台设备上，**本应归在文件夹里的文件，会在「未整理」（根目录）多出一份以 `hash.ext` 命名的副本**
  （`page_id IS NULL` 的语义见 `attachments.rs:643`；根目录视图见 `FileManagerView.tsx:150` 传 `null`）。

**判定依据（2026-09-15 逐条取证，不再依赖真机）**

| # | 事实 | 证据 |
|---|---|---|
| 1 | `attachments` 的主键**只有 `id`**；`hash` 上只有**非唯一索引** ⇒ 同一 hash 天然可多行，`INSERT OR IGNORE` 挡不住 | `db.rs:622-632`（`id TEXT PRIMARY KEY` + `CREATE INDEX idx_attachments_hash`，**没有 UNIQUE**） |
| 2 | 下载路径：新 uuid + `page_id = NULL` + `INSERT OR IGNORE` | `sync.rs::download_one_attachment`（原实现） |
| 3 | 元数据路径：用 `changes` 负载里的 `id` 与 `page_id` | `sync.rs` 的 `do_pull` 附件 upsert |
| 4 | 「未整理」= `page_id IS NULL` ⇒ 第 2 条那行**必然**在根目录多显示一份 | `attachments.rs:643`；`FileManagerView.tsx:150` |
| 5 | 代码库**本来就假设同一 hash 可以有多行**（所以多行不是非法状态，本次修的只是"同步下载制造出来的那一份"） | `attachments.rs:743-755`：只有**零引用**才删盘上字节 |

⚠️ **这条是 Rust 侧独有的**：**Web 引擎的下载路径根本不往 `attachments` 表插行**
（只 `blobStore.put(hash, blob)`，行由 `applyChange` 建）⇒ 所以
**CI 里那个"两设备"验收脚本（`scripts/verify-two-device-sync.mjs`）复现不出它**——
它驱动的是 Web 引擎。想复现必须先让那个 harness 走 Rust 下载路径（目前没有这个能力）。

**已修（B4）**：把兜底插入抽成 `record_downloaded_attachment()`，**只有该 hash 一行都没有时才插** ——
既消掉重复行，又保住兜底语义（服务端有字节、本地没有元数据行时，下载完的字节仍要能被用户看见）。
单测 `downloading_bytes_does_not_duplicate_an_existing_attachment_row` 同时钉住两面：
"有元数据行 ⇒ 仍是 1 行且保留真目录行" 与 "没有行 ⇒ 兜底行照插（`page_id IS NULL`）"。
⚠️ **仍未做真机双设备观感确认**（本机 `cargo test` 加载即死 ⇒ 该单测目前只过编译检查）。

---

## 三、根因（三层）

### 3.1 「差集对齐」这个语义被直接复用到了手机上
`sync_attachments` 是同**一个函数**同时服务桌面与 Android（`sync.rs:1360` 被 `sync_workspace_only` 调用，
后者被 `sync_now:1379` 与 `sync_workspace:1423` 调用）。**没有任何平台分叉**。
桌面上的合理语义，在手机上就是"把 NAS 搬进来"。

### 3.2 全文没有「预算」这个概念
没有体积预算、没有磁盘余量预算、没有流量预算、没有内存预算。
唯一的限制来自**操作系统在失败那一刻的报错**（#6、#10）。

### 3.3 可见性缺口让前两层无法被发现
#16–#20：桌面/安卓既没有附件进度，**连结束态都没有**。
所以"后台默默下了 8 GB"这件事**在 UI 上是一片空白**——用户不可能自己发现，也就不会有人来报。

---

## 四、方案取舍

| 档 | 做法 | 判据 / 代价 | 结论 |
|---|---|---|---|
| **P0** | **修文档口径**（#24 三处） | 零风险、零行为变化；不修就是"下一个离线 OCR"（纪律第 9 条） | ✅ **已完成（2026-09-15）= B1**——实际改了 6 个文件 + 2 个仓（比原估的"三处"多） |
| **P1** | **补结束态 + 附件进度上报**（先修 #19 的地基，再让 Rust 侧上报 `attCurrent/attTotal`） | 不改语义；是 P3「刹车触发要能报告」的前置 | 🟡 **半成品（2026-09-15 核查）**：**结束态已修**（= **B2** ✅）；**"Rust 侧上报附件进度"这一半没做** ⇒ 桌面 / 安卓看不到 `N/M` 与进度条。证据与补法见 **§4.4** |
| **P2a** | **导出 / 另存为改真流式**（未加密时直接 `fs::copy`，别 `read` → `decrypt` → `write`） | 🔴 这是**唯一无条件**发生的那条（§2.6（2）d），**零风险、不改加密格式**，改完即真流式 | ✅ **做（便宜、现在就该做）** |
| **P2b** | **加密开启时的流式化**（导入 / 上传 / 下载） | 流式**静态**加密的前提是**分块 AEAD**，而现有是整块 ⇒ **改格式 = 存量附件兼容**（读路径要同时认两种）。**这是全方案风险最高的一步** | ⏸ **先真机实测是否真 OOM，没实测不动格式** |
| **P3** | **同步预算刹车**：本次下载总量上限 + 磁盘余量下限（硬性），触发即**优雅停止并报告** | 不改语义、不改数据模型；默认值需拍板 | ✅ **已完成（2026-09-15）= C1**，默认值已拍板：磁盘余量下限 **1 GB（硬性）/ 单文件 100 MB / 本轮总量 0 = 只报告不拦截**，见 §4.3 |
| **P4** | **大附件不自动下载 + 「未下载」状态** | 价值高，但**引入新状态**：需要读路径/UI 配合，否则文件看起来"坏了" | ❌ **建议不做，用「每空间开关」替代**（§4.1） |
| **P5** | **网络与时机闸门**（仅 Wi-Fi / 仅充电 / 仅手动） | 需要**新能力**（#9，可行性未评估） | ✅ **已完成（2026-09-15）= C2**：走仓内已有的 Android 原生插件拿**真实**网络类型，零新依赖；见 §4.2 / §4.3 |
| **P6** | **按需取字节 / 每空间开关** | 真解法（"桌面 vs 手机"的正确语义）。**2026-09-15 已立项**；立项后核实的结论**比本行原估的窄**——「行有、字节没有」**已是代码预期状态**（`attachments.rs:434-436`），**不需要新状态 / 数据模型改动** | ✅ **已立项**（[按需取字节立项](2026-09-15-attachment-on-demand-plan.md)，**先做「每空间开关」**） |
| **P7** | **只修文档，行为一律不动** | 最低成本，但 #1/#2/#3 三条伤害照旧 | ❌ 不推荐 |

### 4.1 建议的执行批次（本方案的实际推荐）

**一句话**：**把「刹车」从"优化项"提升为「Android 上架的门槛」；现在只做便宜的，不同时改同步语义。**

三条判据（它们决定了这个排序）：

1. **真凶不是 OOM，是"没有刹车"。** §2.6（2）分清了触发条件 ⇒
   **默认配置下手机同步大文件不会 OOM，它会安静地把设备填满**；而 P2a（导出）是**唯一无条件**的一条。
2. **桌面也中招，但只有手机才"不可接受"。** 填满磁盘在桌面上是麻烦（同分区的 SQLite 会受牵连，
   但仍可清理），在手机上是应用被杀 + 系统空间告警；而"每 5 分钟无网络闸门的自动同步"两边都跑。
3. **手机端还没上架 ⇒ 有一个真实的时间窗。** Android 目前是"能构建已签名 APK + 真机跑通、
   **未对外发布**"（`docs/MOBILE.md:34,60-61`）⇒ 今天没有用户在踩，但**上架第一天就会有人踩**。
   **所以不要现在为了手机大改同步语义，把它排成上架前置项。**

**第一批 —— 现在就做（4 件，都不改同步语义）**

| # | 做什么 | 成本 | 为什么现在做 |
|---|---|---|---|
| **B1** ✅ **已完成<br>（2026-09-15）** | **口径修正**：把"Web 同步**是 stub / 没实现**"这个与代码相反的说法改掉——三处文档/注释 + 跨仓核对表 **#40**（`web.ts:2429/2541/896` 是**完整实现**）。**最终口径由产品裁定**：Web 版**不提供**多设备同步，**实现保留** | 纯文档，≈0 | 已完成，交付见下 |
| | ↳ **实际改了 6 个文件 + 2 个仓**（比原计划的"三处"多，因为查出同源错误还有：`docs/web-sync-boundary.md` **整篇核心论断与代码相反**、`docs/identity-privacy-roadmap.md:18`、社区仓事实纪律第 7 条 + 统一检查项 + 3 处描述性位置、`web.ts` 加决定注释）；另在 `shuyonote-sync-server` 修了 CORS 注释里过时的半句 | — | 提交：ShuyoNote `2e57574`（origin+github 三处 SHA 一致）· 社区仓 `484b6c7` · sync-server `ffe87e1`；`npx tsc --noEmit` 通过、`web.ts` 只增注释 0 删除；`check.mjs` 仍全绿 |
| **B2** ✅ **已完成<br>（2026-09-15）** | **补 `end()`**：`SyncPanel.tsx:214` 调了 `begin()`，而全仓无配对 `end()`（只有 `web.ts:2555/2558` 有）⇒ 桌面/安卓手动同步后**永远停在"正在同步…"** | **一行** | 这是**纯 bug**，且**一物两用**：① P1 的进度上报以它为地基；② ⚠️ **它同时是 P6.1 的前置**——P6.1 要求"中途关掉开关 ⇒ 面板报告*因开关关闭而停止*"，而**没有 `end()` 就没有地方显示停止原因**（与"永远停在正在同步…"是同一个缺口）。详见[按需取字节立项](2026-09-15-attachment-on-demand-plan.md) §四 步骤 0。**交付：`f4562cd`** |
| **B3** ✅ **已完成<br>（2026-09-15）** | **P2a**：导出 / 另存为在**未加密**时改 `fs::copy`，别 `read` → `decrypt` → `write` | 十几行，**无格式变更** | 唯一无条件的整块读；手机上"把传进去的视频下载出来"当场 OOM 的就是它 |
| | ↳ **实际改动**：`attachments.rs` 抽出纯函数 `export_attachment_to(src, write_path, key)` —— `None` ⇒ `fs::copy`（内核态拷贝，RSS 不随文件大小增长）；`Some(k)` ⇒ 保持整块解密（**不做假优化**，见下）。配 2 条单测（未加密逐字节相同 + 加密导出必须解密成明文）。<br>⚠️ **动工前那条前置已核实**（原 §四 P2a 要求"确认 `SaveTarget::write_path()` 在桌面与 Android 两侧都能作为 `fs::copy` 目标"）：`save_target.rs:84-100` 桌面走 `direct(目标路径本身)`、Android URI 走缓存里的**真文件** `…/save/<stem>-<uuid>.part` ⇒ **两边都是真实文件系统路径**，`fs::copy` 都成立（URI 的落地仍由 `commit()` 的流式拷贝负责）。<br>✅ 未加密分支的产物与旧实现**逐字节相同**（旧路径是 `read` → `decrypt(None, …)` 透传 → `write`），有单测钉住 | — | `cargo check --all-targets` = 0 错误；⚠️ **本机 `cargo test` 加载即死（0xc0000139，与改动无关）⇒ 新单测目前只过了编译检查**，见[按需取字节立项](2026-09-15-attachment-on-demand-plan.md) §十一.4 |
| **B4** ✅ **已完成<br>（2026-09-15）** | **先做一次两设备回归**，验证 §2.6（3）那个**重复行** | 只有代码依据、没实测；它决定"要不要单独立项"，**且必须先证实再动代码** |
| | ↳ **实际做法改了**：没有真机也能判定——这条的成立与否**完全由 schema + 两条 INSERT 决定**（`hash` 上没有 UNIQUE ⇒ `INSERT OR IGNORE` 挡不住），于是改为**逐条取证判定成立并直接修掉**，见 §2.6（3）的判定依据表。修法 = 兜底行"**有就不插**"，配一条双向单测。⚠️ 真机双设备**观感**确认仍未做；且 CI 那个两设备脚本驱动的是 **Web** 引擎、**复现不出**这条（Rust 独有）。| — |

**第二批 —— Android 上架前必须做（2 件）**

| # | 做什么 | 关键点 |
|---|---|---|
| **C1** ✅ **已完成<br>（2026-09-15）** | **P3 预算刹车**：磁盘余量下限（硬性、不可关）+ 本次下载总量上限 + 可选单文件阈值；触发即**优雅停止并报告** | 按内容寻址 + `local_set` 每轮重算 ⇒ **停止后再同步自然接着下，不需要断点续传**（现有设计的免费红利）。同时把 `sync.rs:1660` 的 `?` 改成与 `:1646` 一致的"记一笔、跳过"——**否则一次网络抖动就炸掉整轮，也就无法"优雅停止"**。**实际实现见 §4.3** |
| **C2** ✅ **已完成<br>（2026-09-15）** | **P5 网络闸门**（仅 Wi-Fi / 仅手动） | ✅ **"拿不到网络类型"这个前提是错的**：仓里**已经有**一个自有的 Android Kotlin Tauri 插件，加一条命令即可拿到**真实**网络类型——见 §4.2。所以不必退到"只做仅手动"，也**不需要**第三方插件；**绝不用 UA / 平台猜测代替真实网络状态**（那正是 `platform/index.ts:35-38` 警告过的近似错误）。**实际实现见 §4.3** |

**上架 checklist 的判据**：Android 上绑定一个 >10 GB 的空间 → 同步**不会在用户不知情时填满设备**；
**无 Wi-Fi 时不会自动拉取**。

### 4.2 C2 的前置评估：**不需要第三方插件**（2026-09-15 核实）

C2 原话是"`src-tauri` 全仓无网络类型能力 ⇒ 需确认 Tauri 2 有无可用插件；拿不到就只做『仅手动』"。
**前半句的结论对（确实没有），后半句的退路不必走**——因为仓里**已经有一个自有的 Android 原生插件**，
加一条命令就能拿到**真实**网络类型，全程无新依赖：

| 事实 | 证据（file:line） |
|---|---|
| 已有自有的 Android Kotlin Tauri 插件（不是第三方） | `src-tauri/gen/android/app/src/main/java/cn/shuyo/shuyonote/ShuyoFsPlugin.kt:39-50`（`@TauriPlugin` + `@Command fun pickedFileInfo`） |
| ⚠️ **它是生成物**，源头在仓内的 Node 脚本里 | 同文件 `:18-21`「由 `scripts/android-mobile-shell.mjs` 注入，别手改 gen/」+ `gen/` 在 `.gitignore` ⇒ **改插件要改脚本，不是改 gen/** |
| 脚本里持有 Kotlin 模板与命令名 | `scripts/android-mobile-shell.mjs:88`（路径）、`:299`（与 Rust 侧对得上）、`:314`（`ShuyoFsPlugin` 模板开头） |
| 脚本还负责注入 manifest 权限，且**照抄现成写法** | `:442-489` `injectManifest()`（"找不到才插"）；`:580-591` `--check` 会校验注入结果 |
| 现有权限里**没有** `ACCESS_NETWORK_STATE` | `src-tauri/gen/android/app/src/main/AndroidManifest.xml:5-6` 只有 `REQUEST_INSTALL_PACKAGES`、`INTERNET` |

⇒ **落地方案（三处，都在仓内、Android-only）**：
① `scripts/android-mobile-shell.mjs` 里给 `ShuyoFsPlugin` 加 `@Command fun networkType()`（读
`ConnectivityManager.getNetworkCapabilities()` 的 `TRANSPORT_WIFI` / `TRANSPORT_CELLULAR` /
`NET_CAPABILITY_NOT_METERED`），并在 `injectManifest()` 的权限清单里加
`android.permission.ACCESS_NETWORK_STATE`（**`ACCESS_NETWORK_STATE` 是普通权限、不弹窗**）；
② Rust 侧照 `src-tauri/src/android_fs.rs` 的既有桥接写法加一条命令（**两边类名/命令名由 `--check` 一起核**）；
③ 前端仅用来**决定要不要自动同步**，拿不到值时**回退成"不自动拉取"**（fail-safe，绝不猜）。

⚠️ **为什么不要第三方插件**：搜到的候选里 `tauri-plugin-network-manager` 是 **Linux-first**
（NetworkManager over D-Bus），与 Android 无关；其余多为桌面扫描类或不活跃。
而"自己加一条 Android 命令"这条路**本仓已经跑通一次**（`ShuyoFsPlugin` 就是），零新依赖、可控。

### 4.3 C1 + C2 落地记录（2026-09-15）

**C1 预算刹车**（默认值由发布者当日拍板：磁盘余量下限 **1 GB（硬性）** / 单文件 **100 MB** / 本轮总量 **0 = 只报告不拦截**）

| 层 | 位置 | 内容 |
|---|---|---|
| 磁盘余量 | **新增** `src-tauri/src/disk.rs` | `available_bytes(path)`：Unix/Android 走 `statvfs`（用 **`f_bavail`** 而不是 `f_bfree`——保留块我们用不到，算多了刹车就踩晚），Windows 走 `GetDiskFreeSpaceExW`。`libc` 本就是直接依赖、`windows-sys` 本就**在 lockfile 里**（0.61.2）⇒ **零新增供应链**，只是显式声明 |
| 设置 | `sync.rs` | 四个键存 `meta.sync_state` 的 KV（**不新建表**）：`sync_disk_floor_mb` / `sync_max_file_mb` / `sync_max_run_mb` / `sync_wifi_only`；`SyncBudget` + `clamped()` + `read_budget()`；命令 `get_sync_budget` / `set_sync_budget`（**回显夹取后的值**） |
| 引擎 | `sync_attachments` | 三道闸门：**磁盘余量（硬性，停止）** / **单文件阈值（跳过该件、继续）** / **本轮总量上限（停止）**；停止原因进 `attachments_paused_reason`；把**单件下载**抽成 `download_one_attachment()`，两侧循环的 `?` 改成"记一笔、跳过" |
| 报告 | `SyncReport` / `WorkspaceSyncResult` | `attachments_paused_reason` / `attachments_skipped_too_large` / `attachments_failed` / `attachments_bytes_downloaded` |
| 前端 | `commands.ts` / `api.ts` / `web.ts` / `SyncPanel.tsx` / `App.css` | 契约 + Web 同构实现（KV 用同名 `sync_state` 表）+ 面板里的「同步预算」折叠区与停止原因文案 |
| 测试 | `sync.rs` / `disk.rs` | 4 条：默认值就是拍板的那三个数 / 往返与**垃圾值退回默认** / **磁盘余量下限不可关**（写 0 与读出都要夹）/ 本机能查到正的剩余空间 |

**三个关键设计决定（都写进了代码注释）**

1. **文件大小不需要改服务端**：`GET /attachments` 只回 `{hash, mime}`（`sync-server/src/attachments.rs:22-31`），
   而且下载走 `Body::from_stream` ⇒ **没有 `Content-Length`**，HEAD 也问不出来。但
   **`attachments.size` 本来就随 `changes` 同步到本地**（`attachments.rs:348` 的 payload 带 `size`），
   也正是"看得见但打不开"那条状态的来源 ⇒ **本地就是有大小**，拿它做下载前判断即可。
   拿不到那一行时才退化成"不预判"（交给另外两道闸门兜底）。
2. **磁盘余量查不到时 fail-open**（只记日志、不拦）：这里**没有安全的默认方向**——fail-closed 会让
   任何查询失败都变成"同步坏了"，比少拦一次严重。Android 是这条闸门的主要目标平台，它有真实实现。
3. **`unknown` 与 `n/a` 必须分开**（C2 的契约）：`n/a` = 非 Android、闸门**不适用** ⇒ **不拦**；
   `unknown` = Android 上真查不到 ⇒ **不自动拉取**。两者混同就会**把桌面的自动同步也一起关掉**。

**C2 网络闸门**

- Kotlin `@Command fun networkType`（`ConnectivityManager.activeNetwork` + `NetworkCapabilities` 的
  `TRANSPORT_WIFI`/`TRANSPORT_CELLULAR`/`TRANSPORT_ETHERNET`）+ `ACCESS_NETWORK_STATE`
  （**普通权限，安装即授予、不弹窗**），两样都由 `scripts/android-mobile-shell.mjs` 注入，
  并由 `--check` 把关（含 Rust↔Kotlin 的**每一个**命令名）。
- Rust `src/net.rs`：`#[tauri::command] network_type()`，Android 走真查询、其它平台回 `"n/a"`。
- 前端 `useAutoSync`：`wifi_only` 为真且网络类型不是 `wifi`/`ethernet`/`n/a` ⇒ **跳过这一轮自动同步**。
  **手动点「同步」不走这条检查**（用户明确要求就该照做）。
- 面板上的开关**只在真能查到网络类型的平台上出现**（`netKind !== "n/a"`）——
  判据用的是**真实查询结果**，不是 UA/平台近似。

**验证状态（诚实版）**

| 项 | 结果 |
|---|---|
| `npx tsc --noEmit` / `pnpm test`（70 文件 656 用例）/ `pnpm check:web-commands`（Rust 218 ↔ web 221）| ✅ 全绿 |
| `cargo check --all-targets` | ✅ 0 错误 0 警告（**含新增单测的编译检查**） |
| `node scripts/android-mobile-shell.mjs --check` | ✅ 通过（新增的 Kotlin 命令、manifest 权限都在判据里）|
| **Rust 单测执行** | ⚠️ **本机 `cargo test` 加载即死**（`0xc0000139`，与本次改动无关）⇒ 新单测只过了编译检查 |
| **真机验收**（磁盘余量停止 / 仅 Wi-Fi 不自动拉 / 权限不弹窗） | ⏳ **未做**，需要 Android 真机 + 一个 >10 GB 的空间 |

---

### 4.4 ✅ P1 两半都补齐了（结束态 + **Rust 侧进度上报**）（2026-09-15）

§4.1 的 P1 是**两件事**：① 修结束态（`begin()` 没有配对的 `end()`）；② 让 **Rust 侧**上报
`attCurrent/attTotal`，面板才能显示 `N/M` 与进度条。

**① 结束态** ✅ = **B2**（`f4562cd`）：同步结束会有结果文案，不再永远停在"正在同步…"。

**② 进度上报** ✅（2026-09-15 补齐）。补齐前它是**半成品**，证据（两次 grep）曾是：

| 查什么 | 当时的结果 |
|---|---|
| Rust 全仓有没有 `attCurrent` / `attTotal` | **一处都没有** |
| 谁在调 `useSyncStatus.setProgress` | **只有 `web.ts`**（Web 引擎）⇒ 桌面 / 安卓这条链上没人上报 |

⇒ 现象：桌面 / 安卓点「同步」后**看不到** `N/M` 与附件进度条；只有 Web 引擎会填。

**补法（就是按当初写下的方案做的）**：

| 层 | 位置 | 内容 |
|---|---|---|
| Rust | `sync.rs::AttachmentSyncProgress` + `emit_attachment_progress()` | 每传一件附件前 emit `attachment-sync-progress`；字段 **`#[serde(rename_all = "camelCase")]`**，与前端 store 的 `phase/message/attCurrent/attTotal/attName` 逐个对齐（**不在两侧各翻译一次字段名**）；上传侧 `attName` 放 mime、下载侧放 hash 前 8 位，文案与 `web.ts` 的两句**逐字一致** |
| 前端 | **新增** `hooks/useSyncProgress.ts`，在 `App.tsx` 与 `useAutoSync` 并列挂载 | 监听后写进 `useSyncStatus.setProgress`。⚠️ **挂 App 级而不是 SyncPanel 里**：自动同步在面板关着时也会跑，进度必须照样被记录 |

⚠️ 有个细节**不需要**额外处理（当初我担心过）：不必在"优雅停止"时补发一条收尾进度——
面板的进度条只在 `syncStatus.syncing` 为真时渲染，而 `end()`（B2）会把它翻成假并切到结果文案，
所以停下来的那一刻进度条不会僵在半路。

⚠️ **仍未做真机验收**：这条要装到真机上、同步一个含多个附件的空间才能看见 `N/M` 走动，
本机没有 Android 构建能力（见 §4.5）。

---

### 4.5 真机验收记录（2026-09-15 深夜 · HUAWEI Mate 40 / OCE-AN10 / Android 12）

**这一节是"计划里写着需要真机"的那些条的答案。** 结论：**五条主要验收通过，并当场揪出三个只有真机会暴露的问题（都已修）。**

#### 4.5.1 包怎么上的机器（顺带把"本机构建不出来"这条落实到了具体原因）

| 事 | 结果 |
|---|---|
| 本机（Windows）构建 Android | ❌ **仍然不行，但这次定位到了确切原因**：`openssl-sys` 走源码构建，`perl ./Configure` 失败——**Git for Windows 自带的是精简版 Perl，缺 `Locale::Maketext::Simple`**（`Can't locate Locale/Maketext/Simple.pm`）。本机没有完整 Perl（无 msys64 / strawberry / cygwin），有一份 **WSL Ubuntu 2**（perl 完整、NDK 可见、rsproxy 可达）但**里面没有 Rust** ⇒ 要么装完整 Perl、要么在 WSL 里装 Rust 交叉编译，都不是当晚能顺手做的 |
| 走 CI 出包 | ✅ **可行且已验证**：`android.yml`（手动或 push 触发）产出 **用正式密钥签名**的自检包。本机把 `api.github.com` 强制到真实 IP `20.205.243.168`（DNS 被污染）后用 REST API 触发/下载 |
| 覆盖安装保住数据 | ✅ **逐字节核过签名**：设备上已装包的证书 SHA-256 = `6ee89e6f…457a88` = `~/.shuyonote-release-keystore/README-必读.md` 里记的正式指纹；CI 包同一把 key ⇒ `adb install -r` 成功，`firstInstallTime` **未变**（2026-09-13 21:55:55），用户数据没动 |
| 版本 | 1.91.1 → **1.91.2**；`ACCESS_NETWORK_STATE` 出现在设备上（**C2 清单注入的机械验证**） |

#### 4.5.2 验收环境：不碰生产数据

设备上那个空间的服务器本来就填着 `http://localhost:8787`（早先测试留下的），于是：
`adb reverse tcp:8787 tcp:8787` ⇒ **手机上的 localhost:8787 就是本机的测试服务端**；
用服务端 CLI `--issue-device-key` 签一把密钥，手机侧只填「服务器 + 设备密钥 + 空间 id」三样即可。
夹具由本机脚本灌入（3 个附件 + 后续几个），**全程没有碰 `shuyo.cn` 上的任何真实数据**。

UI 无法用 `adb shell input` 驱动（华为 WebView 收不到合成触摸、`input text` 进不了输入框；
BACK/ESC 会被应用当成"关浮层"）。改用 **WebView 的 CDP 调试口**
（`@webview_devtools_remote_<pid>` → `adb forward` → `Runtime.evaluate`），
**在真实页面上填真实输入框、点真实按钮**（React 受控输入用原生 setter + `input` 事件）。

#### 4.5.3 验收结果

| # | 验收项（本方案/立项书里的原话） | 结果 | 证据 |
|---|---|---|---|
| 1 | **C2：Android 上拿到真实网络类型** | ✅ | 面板里出现了「只在 Wi-Fi 下自动同步」这一项——**它只在 `network_type !== "n/a"` 时渲染**，而桌面/Web 回 `n/a` |
| 2 | **C2：`ACCESS_NETWORK_STATE` 注入** | ✅ | `dumpsys package` 里能看到该权限；Kotlin 本机编译过（`gradlew :app:compileUniversalDebugKotlin`） |
| 3 | **C1：默认值就是拍板的三个数** | ✅ | 面板三个下拉的初值直接读出来是 **1024 / 100 / 0**（`get_sync_budget` 真机往返） |
| 4 | **C1：单文件阈值会"跳过而不是停"** | ✅ | 60 MB 待下载件 + 上限 50 MB ⇒ 「同步**完成**（**1 个超过单文件上限**）」，同步没有停 |
| 5 | **C1：一件失败不炸整轮** | ✅ | 同一次同步里如实报出「**8 个传输失败**」而整轮照常完成（旧代码会在第一个失败处炸掉） |
| 6 | **P6.1：开关默认开、切换即落库、文案正确** | ✅ | 默认勾选；点一下变关并落库；应用自己的状态行是「已关闭附件同步：同步时只走笔记内容，不传附件文件」 |
| 7 | **P6.1：关掉后"元数据到、字节不到"** | ✅ | 关着开关同步 ⇒ 「未上传 6 个 / 未下载 1 个」，新文件出现在文件管理器 |
| 8 | **P6.2：「未下载」标记只给缺字节的那一行** | ✅ | 10 行里**只有** `验收开关验证.md` 是 `missing: true`，其余 9 行都有字节 |
| 9 | **P6.3：按需下载，且**不受开关限制**** | ✅ | 关着开关点该行的「从服务器下载到本机」⇒ 提示「已下载」、标记消失、按钮变回「下载」 |
| 10 | **P6.3 续：点开一个"未下载"的文件会自动取回再渲染** | ✅ | 点 `验收打开验证.md` ⇒ 预览打开并**渲染出了文件内容**（旧行为只会给一句"文件内容缺失"） |
| 11 | **B4：同一附件不会被插成两行** | ✅ | 走完"元数据行 + 字节下载"这条路后，文件列表里该文件**只有一行**（没有多出 `hash.md` 副本） |
| 12 | **C1：磁盘余量下限** | ⛔ **这台机器上验不了** | `/data` 空闲 **138 GB**，而界面里下限最大只有 5 GB（Rust 上限 1 TiB）⇒ 触发不到。**代码路径本身仍未在真机跑过** |
| 13 | **C1：本轮下载总量上限** | ✅ **第二轮验收补验** | 2×260 MB 待下载件 + 上限 500 MB ⇒ 「同步**已停止**：已达「本次下载总量上限」…（**未下载 2 个** / 本次下载 **328.0 MB**）」——**已下的保留，下一个会顶穿就根本没开始**（正是"把这一件算进去再判"） |
| 14 | C2 的"仅 Wi-Fi"闸门 | ✅ **并因此发现 bug #3**（见下） | 反证：Wi-Fi 关着 + 自动同步 10 秒 ⇒ 标记文件**照样进来了** |

#### 4.5.4 真机当场暴露的三个问题（都已修）

| # | 现象（真机原话/观测） | 根因 | 修法 |
|---|---|---|---|
| 1 | 开关**在同步之前**就是关的，面板却报「同步**已停止**：**途中**关闭了附件同步」 | 入口只设了件数快照，**循环照进** ⇒ 第一轮循环的开关检查立刻把 `paused` 置真 | 两侧循环开头 `if !att_on { break; }`：入口关 = 稳态（不计 paused、照报件数），只有**中途翻开关**才是"停止"；顺手补上传侧漏掉的 `paused_reason` |
| 2 | 打开一个"未下载"的文件时**同一份字节被下两次**，且两个下载抢同一个 `.part`：toast 里同时出现 `取回文件失败：No such file or directory (os error 2)` 与 `已从服务器取回文件` | ①`filePreview` 与预览弹层各调一次 `ensureAttachmentBytes`；②Rust 的临时文件名固定是 `<hash>.part` | ①前端把**同一 hash 的并发调用合并**（in-flight map）；②Rust 临时文件名改 `<hash>.<uuid>.part`（`.part` 结尾仍被 `local_set` 排除） |
| 3 | Wi-Fi 关着、自动同步设成 10 秒，**照样在蜂窝上拉**（标记文件进来了） | 仓里有**两条**自动同步路径：`useAutoSync`（固定 5 分钟，我加了闸）与 **`App.tsx` 按面板间隔跑的定时器**（没闸） | 判据抽成 `lib/syncGate.ts`，**两条路共用一处**（各写一份迟早会漂） |

#### 4.5.5 顺带发现的既有缺口（**不是本次改动的 bug**，记下来别丢）

1. **Rust 上传侧曾缺"存储名必须是内容哈希"这道校验** ⇒ 设备上残留的 9 个**两字符名**文件
   （看着像旧版本"忘了分桶目录"写出来的）会被当成本地待上传附件：服务端 `valid_hash` 直接 400
   并在**读完 body 之前关掉连接** ⇒ 客户端只看到 `error sending request for url (...)`——
   与真实原因毫不相干，而且白传一遍大文件（真机 logcat 里 9 条）。
   ✅ **已当场修并复验**（见 §4.5.7）：现在是一行清晰日志 + 跳过，`error sending request` **归零**。
   ⚠️ 但**遗留文件名本身没被清理**（那是数据问题，要另做决定）；
   另：`web.ts` 有一份"旧 16 位 FNV 哈希 → SHA-256"的迁移，**Rust 上传侧仍然没有**——
   这两件事都不是本次改动引入的。
2. **文件管理器不会因后台同步而刷新**：`loadFiles()` 只在切换文件夹 / 导入 / 删除后跑。后台同步拉进来的新文件要**重开或重载**才看得见（当晚我为了验第 8 条必须重载一次）。
3. 设备上那个空间**没有配置可用的同步账号**（服务器是早先测试留的 localhost），所以本轮是**用本机测试服 + 设备密钥**验的——**没有碰生产**。

#### 4.5.6 仍未验的

- C1 的**磁盘余量下限**与**本轮总量上限**（见上表 12/13：一个被 138 GB 空闲挡住、一个要造 500 MB 以上夹具）；
- **P1 的进度条**、以及上面三个修复的**真机复验**——都放在含这些改动的 CI 包上做（`fe877b5`，第四轮）；
- 双设备/双机场景（B4 的另一半）依旧没有真机对手。

#### 4.5.7 第二轮验收：三个修复 + P1 + C1 第三道闸门（2026-09-15 深夜，第二/三个 CI 包）

**为什么还要一轮**：第一轮当场揪出三个问题（§4.5.4）——修完必须**在同一台真机上复验**，
否则"修了"只是代码事实，不是行为事实。

| 复验项 | 结果 | 真机证据（面板原话 / 观测） |
|---|---|---|
| **修复 1：入口关闭不再误报"途中停止"** | ✅ | 先在面板里把开关关掉、再点同步 ⇒ **「同步完成（未上传 13 个 / 未下载 2 个）」**（不再是「已停止：途中关闭了附件同步」）；件数照报 |
| **修复 2：同一件不再被并发下载两次** | ✅ | 点开一个"未下载"的 md ⇒ 预览**渲染出内容**，toast **只有一次**「已从服务器取回文件」，**`No such file or directory (os error 2)` 彻底消失**（修复前每次必现） |
| **修复 3：C2 闸门覆盖第二条自动同步路径** | ✅ **A/B 反证** | Wi-Fi 关 + 自动同步 10 秒：新标记 `门禁验证D.md` **没进来**（列表 22 行）；打开 Wi-Fi 后**自己进来了**（23 行）。⚠️ 同一实验在修复**前**跑过一次：标记 `门禁验证B.md` **照样进来了** ⇒ 当初那条路确实绕过闸门 |
| **上传名校验（顺带修）** | ✅ | logcat 现在是 `[sync] 附件 ac 的存储名不是 SHA-256（历史遗留文件？）——跳过上传` ×13，`error sending request` **计数 0** |
| **P1：安卓/桌面也能看到 N/M 进度** | ✅ | 3×90 MB 待下载件 ⇒ 面板依次出现 **「正在下载附件（1/4）1/4」→（2/4）→（3/4）**，随后是结果行（**进度条不会僵在半路**——`end()` 收尾）|
| **C1：本次下载总量上限** | ✅ | 见 §4.5.3 第 13 行：328 MB 已下 + 下一个 260 MB 会顶穿 ⇒ **根本没开始**就停，报「已达「本次下载总量上限」…未下载 2 个」 |

**结论**：C1 的三道闸门里 **两道在真机验证通过**（单文件阈值、本轮上限），第三道（磁盘余量）
在这台设备上**无法触发**（138 GB 空闲 vs 界面最大 5 GB）——所以它仍然**只有代码依据**。
P6.1/P6.2/P6.3、P1、B4 的修复、C2 的闸门**都拿到了真机行为证据**。

### 4.6 第三轮验收（2026-09-15 深夜 · 第四个 CI 包）：又修两条，并发现一条**新的数据质量**问题

#### 4.6.1 修掉的两条（都来自 §4.5 那条线）

| # | 问题 | 为什么只有真机看得见 | 修法 | 复验 |
|---|---|---|---|---|
| 5 | **自动同步没有配对 `end()`** ⇒ 面板永远停在"正在同步…" | P1 之后 Rust 每传一件附件就 emit 进度，而进度会 `setProgress(syncing=true)`；手动同步有 `end()`（B2 修的），**自动的两条路（`useAutoSync` 与 `App.tsx` 的定时器）都没有** ⇒ 一旦自动同步真的传了附件，store 就再也不结束 | 判据收成一处 `store/syncStatus.ts::withSyncStatus(msg, run)`，三段调用共用（含**设置页那条也漏的**同步路径 `SettingsDialog::syncGroup`） | ✅ **真机对照**：修复前实测状态行**冻在**「正在下载附件（1/2）1/2」不动（自动同步是关的、也没同步在跑）；修复后同样的"自动同步传完一个 5 MB 附件"场景，状态行读数两次都是**空**。⚠️ **设置页那条没能在真机上点到**：手机布局下设置里「同步身份（按服务器分组）」显示为空（而同步面板里同一个 profile 明明有 server_url）⇒ `同步该组` 按钮压根没出现。**没能区分**这是移动端布局差异还是分组列表的问题，**未深查**；该路径与另两条共用同一个 `withSyncStatus`，**没有独立的行为证据** |
| 6 | **同步结束后文件管理器不刷新** | 「未下载」标记要**重载页面**才看得见——而那正是 P6.1+P6.2 的主流程（关开关 → 同步 → 看哪些没下来）；第一轮验收时我被迫手工重载 | 文件管理器订阅 `useSyncStatus` 的**下降沿**触发一次 `loadFiles()`；判据取 store（三条路都配对 begin/end）⇒ 一处挂载全覆盖 | ✅ **真机**：同步前 26 行 → 点同步 → **500 ms 内**新文件 `刷新验证新件2.md` 自己出现（带「未下载」标记），**全程没有重载页面** |

#### 4.6.2 ⚠️ 新发现（**未修，属数据语义决定**）：同一个文件可能有两行

真机上现在有 **4 个 raw-hash 命名的行**，每个都对应一个**已经存在**的正常行：

```
8381038c…8ccd.bin   ← 与「刷新验证新件.md」同一个 hash
d38aa62d…6c3ed.bin  ← 与「刷新验证.md」同一个 hash
bd3fc2de…48dd7.bin  ← 与「刷新验证大件.bin」同一个 hash
b6cd69dd…af66c.bin  ← 与「刷新验证待下.md」同一个 hash
```

**机制（和 §4.5 里 B4 修掉的那条**不是**同一条路）**：
下载循环的输入是**服务端的 hash 清单**（`GET /attachments`），它**不依赖 `changes` 游标** ⇒
只要"字节先到、元数据后到"，`download_one_attachment` 的**兜底插入**就会先建一行
`<hash>.<ext>` / `page_id = NULL`（落在「未整理」）；等元数据行随 `changes` 到了，
`do_pull` 的 upsert 又插一行**正常名字**的 ⇒ **一份文件两行**。
本次测试里"字节先到"是我把同一个 profile 改绑到新空间后**游标 `last_pulled_seq` 是旧的**造成的
（新空间 seq 从 1 开始，`since=旧值` 把变更全跳过），但**这个顺序在真实场景里也会出现**
（新设备先拉附件清单、或游标落后于服务端）。

⚠️ **为什么我没有当场修**：修法要先定**语义**——收到元数据行时，对"同一 hash 的兜底行"
是**改写成元数据行**（adopt：把 `id`/`name`/`page_id` 换成 payload 的）、还是**删掉兜底行**？
两者都动数据，而且**修完之后不容易复现验证**（元数据已经落库了，重同步不会再触发"adopt"）。
⇒ 留给清醒时的决定。**修法草图**：`do_pull` 的附件 upsert 在 INSERT 前先
`UPDATE attachments SET id=?, name=?, page_id=? WHERE hash=? AND page_id IS NULL AND name LIKE hash||'.%'`，
`web.ts` 的 `applyChange` 同步改。

⚠️ 顺带一处**小的**：那些兜底行的扩展名是 `.bin`，而它们在服务端其实是 `text/markdown`
⇒ `ext_from_mime` 的映射表里**没有 `text/markdown`**（落到默认 `bin`）。

#### 4.6.3 顺带发现的产品疑点：改绑空间不会重置同步游标

`set_sync_profile` 只改 `server_url` / `space_id` / `token`，**`last_pulled_seq` 原样保留**
⇒ 把一个空间从 A 服务端改绑到 B（或换空间）后，设备会用**A 的游标**去 B 上"增量拉"，
**把 B 里的历史变更全部跳过**（本次实测：「拉取 0」，而服务端明明有一条新变更）。
⇒ 这是**真实可踩**的坑（个人自建 → 团队空间是最常见的改绑）。**未修**，也留给决定：
正确做法大概是"改绑时把游标归零并做一次全量拉取"，但那会让一次改绑拉下整个空间。

---

**明确不做 / 推迟**

| 项 | 决定 | 理由 |
|---|---|---|
| **P2b（加密开启时的流式化）** | ⏸ **先实测，不动格式** | 流式**静态**加密的前提是分块 AEAD ⇒ 改格式 = 存量附件兼容（读路径要同时认两种，否则老用户附件全部解不开）。**判据：真机 + 开加密 + 1–2 GB 文件，先看是否真的 OOM。没实测就不动格式。** |
| **P4（「未下载」状态）** | ❌ **不做，用「每空间开关」替代** | 引入"未下载"要连带回答一串数据模型问题；而"每空间开关"成本极低、无新状态、立刻可用 |
| **P6 主体（按需取字节）** | ✅ **已立项**（2026-09-15） | 见 [按需取字节立项](2026-09-15-attachment-on-demand-plan.md)：**先只做"每空间开关"（P6.1）**；立项时核实到「行有、字节没有」已是代码预期状态 ⇒ 比原估更窄 |
| **重复行** | ✅ **已判定成立并已修（B4）** | 这条由 schema + 两条 INSERT **唯一决定**，不需要真机 ⇒ 取证判定成立后直接修掉（§2.6（3））。真机观感确认仍待做 |

> **P4 之所以不能塞进第一批**：一旦引入"未下载"，就必须回答——打开一页时图片没下载怎么办
> （占位？自动取？）、导出时缺字节怎么办、`cleanup_orphan_attachments` 会不会把"只有元数据、
> 没有字节"的记录当孤儿清掉、跨设备如何表达"这台有那台没有"。**这些是数据模型层面的问题，
> 不该塞进第一批。**
>
> **不做也要记住的一条**：**文件删除是硬删**（`attachments.rs:714` 的 `DELETE` + `attachments` 表
> **无 `deleted_at` 列**）⇒ 以后设计"未下载 / 删除"时**不能假定有回收站兜底**。

---

## 五、实施步骤

### P0 修文档口径（零风险，可单独发）

> ⚠️ **B1 的口径已由产品裁定（2026-09-15）：Web 版不开多设备同步，现有 web 同步代码保持。**
> 所以最终口径是「**Web 版不提供多设备同步**（产品决定）」，**同时不许说"是 stub / 没实现"**
> （与代码相反），**且那段实现按决定保留、不要删**。下表是本次实际落地的措辞。

| 位置 | 改成 |
|---|---|
| 社区仓 `docs/community/shuyo-community-zhihu-content-2026-09.md` 事实核对表 **#40**（+ 事实纪律第 7 条 + 统一检查项 + 3 处描述性位置） | "**Web 版不提供多设备同步**（产品决定）"；两条禁止：🚫 不许写"是 stub / 没实现"（`web.ts:2429/2541/896` 都是**完整实现**）、🚫 不要据"没实现"把那段代码删掉 |
| `docs/MOBILE.md:18` | 从"❌ 是 stub"改为"**不提供**（产品决定）……但 `web.ts` 是**完整实现、按决定保留**，别当死代码删掉" |
| `src/App.css:1221` 注释 | 从"Web 版不支持多设备同步"改为"**不提供**多设备同步（产品决定）"+ 指向 `web.ts` 的完整说明 |
| **`src/lib/platform/web.ts`（本次新增）** | 在 `sync_now` 上方加**决定注释**：为什么不提供、为什么保留、三条禁止、以及"这段到底还会不会跑"（`useAutoSync` 仍会调用，但没有可用入口配置 profile） |

**验收**：① 同步相关的"是 stub / 没实现"**不再有任何断言**——`grep` 仍会命中，但只允许两类：
**引用被纠正的说法**、以及 `capabilities.ts:32` / `capabilities.test.ts:38` 里讲 **email** 的那两处
（email 在 Web 上确实会抛错，见 `web.ts:1119-1122`）；② 三处口径**一致地写"不提供"**（而不是"没有实现"）；
③ `web.ts` 的实现在位——`npx tsc --noEmit` 通过，且 `git diff --numstat` 显示该文件**只有新增注释、0 删除**。

> ⚠️ **这条口径的关键是"分清两个问题"**（别再改来改去）：
> ①「**产品开不开**」——**不开**（2026-09-15 裁定），所以写"**不提供**"是对的；
> ②「**代码有没有**」——**有，而且按决定保留**，所以写"**是 stub / 没实现**"是错的（与代码相反）。
> 此前那句"理由要从『不支持』改成『不推荐』"，是**只看了第②个问题**得出的结论，
> **现已被裁定修正**：说法用"**不提供**"，理由仍是"浏览器存储会被系统回收、不适合当唯一副本"。
> 两个问题分开答之后，口径就稳定了：**既不谎称有问题、也不谎称有能力。**

### P1 补结束态 + 附件进度上报

**P1-a：修 #19（`end()` 缺口）——先做这个，否则进度上报建在坏底座上**

**文件**：`src/components/SyncPanel.tsx`（Modify: `syncOne`，`:207-234`）

在 `finally` 里补 `end()`：

```ts
    } finally {
      setSyncing(false);
      // Rust 侧（桌面 / Android）不会自己调用 end()，此前只有 web.ts 调，
      // 于是手动同步后 store 的 syncing 永远是 true —— 面板会一直显示"正在同步…"，
      // 而完成文案（上面的 setStatus）永远不显示。
      useSyncStatus.getState().end(statusErr);
    }
```

**验收**：
```bash
pnpm test          # 现有测试须全绿
pnpm dev           # 桌面手动同步一次 → 面板应显示「同步完成：上传 X / 拉取 Y」，而不是「正在同步…」
```
**期望**：同步结束后进度区消失、结果文案出现。

**P1-b：让 Rust 侧上报附件进度**

**文件**：`src-tauri/src/sync.rs`（Modify: `sync_attachments`，`:1615-1690` 的下载循环与 `:1572` 的上传循环）

**照抄仓库里已有的两个先例，别新造机制**：

| 先例 | 位置 | 形状 |
|---|---|---|
| 导入/导出进度 | `src-tauri/src/workspace_io.rs:36-49` 的 `emit(app, phase, done, total, bytes, message)` → 事件 `workspace-progress` | `{phase, done, total, bytes, message}` —— 与附件同步需要的字段**几乎一致** |
| **附件**导入进度 | `src-tauri/src/attachments.rs:539-549`：给 `copy_and_hash` 传一个**进度回调**，回调里 `app.emit("attachment-import-progress", ImportProgress { index, total, name, done, size })` | **同文件同领域**的现成范式：把回调穿进拷贝循环 |
| 节流 | `src-tauri/src/backup.rs:88-89`（注释原话 "Throttle emits (every file)"） | 大循环里**不能每字节 emit** |

前端侧也是现成的：`src-tauri/src/ai.rs:250` 的注释写明"前端通过 `platform.event.listen` 订阅"，
落地就是把事件写进 `useSyncStatus.getState().setProgress(...)`（`src/store/syncStatus.ts:37`）。

> ⚠️ **别被 `useSyncStream.ts` 误导**（我第一版就写错了）：它是 **Web 专属**的
> 服务端 SSE 订阅，`useSyncStream.ts:15` 第一行就是 `if (isDesktopPlatform()) return;`，
> 注释 `:5` 写明"Desktop builds keep their auto-sync timer (reqwest); **this is a no-op there**"。
> **它不是 Tauri 事件通道**，桌面/安卓的进度必须走 `app.emit`。

**`attTotal` 必须先算出来再循环**（现在连总数都没有）：
```rust
let to_download: Vec<&RemoteAttachment> = remote.items.iter()
    .filter(|i| is_valid_attachment_hash(&i.hash) && !local_set.contains(&i.hash))
    .collect();
let att_total = to_download.len();   // ← 先有总数，UI 的 N/M 才有意义
// 循环内：emit(phase="attachments", done=i+1, total=att_total, bytes=running, message=&hash)
```
**验收**：桌面/安卓手动同步一个含多个附件的空间 → 面板出现 `N/M` 计数与进度条（`SyncPanel.tsx:795-808` 现有的 UI 即可复用，**不需要改前端**）。

### P2a 导出 / 另存为改真流式（第一批 B3）✅ **已落地 2026-09-15**

**文件**：`src-tauri/src/attachments.rs`（Modify: `copy_attachment` `:374-386`）

✅ **实际实现**：把这段决策抽成**纯函数** `export_attachment_to(src, write_path, key: Option<&[u8;32]>)`，
`copy_attachment` 只负责找路径 / 建 `SaveTarget` / `commit()`。抽出来的理由和 `read_bytes_at` 一样：
**能单测**（`copy_attachment` 本身要 `AppHandle`，构造不出来）。配 2 条单测：
① 未加密导出**逐字节相同**且不动源文件；② 加密导出必须**解密成明文**。

现在**无论是否加密**都是 `std::fs::read` 整块读 → `decrypt_attachment_bytes` → `std::fs::write` 整块写；
而 key 为 `None` 时 `decrypt_attachment_bytes` **原样返回**（`security.rs` 里 `encrypt/decrypt(None, ..)` 的测试）
⇒ **未加密时这一读一写纯属白费**，可以直接按文件复制：

```rust
let target = crate::save_target::SaveTarget::new(&app, &dest_path, "shuyonote-att")?;
match key {
    // 未加密：磁盘上就是明文 ⇒ 直接文件级复制，内存与文件大小无关
    None => { std::fs::copy(&p, target.write_path()).map_err(|e| format!("复制失败: {e}"))?; }
    // 加密：仍是整块解密（要真流式得改加密格式，见 P2b）
    Some(k) => {
        let raw = std::fs::read(&p).map_err(|e| e.to_string())?;
        let plain = crate::security::decrypt_attachment_bytes(Some(&k), &raw)?;
        std::fs::write(target.write_path(), &plain).map_err(|e| format!("复制失败: {e}"))?;
    }
}
target.commit()
```

- **为什么独立成档**：它是**唯一无条件**发生的一条（§2.6（2）d），而修复**不碰加密格式** ⇒
  风险与 P2b 差一个量级，不该被"P2b 风险最高"这个标签拖住。
- **判据**：走 `FileManagerView.tsx:538` 的「下载」，未加密时导出 2 GB 文件 ⇒ **RSS 不随文件大小增长**。
- ✅ **动工前那条前置已核实（2026-09-15）**：`SaveTarget::write_path()` 在桌面与 Android（`content://`）
  两侧都能作为 `fs::copy` 的目标——`save_target.rs:84-100`：桌面 `direct()` 的 `write_path` 就是**目标路径本身**；
  URI 目标则是缓存目录里的**真文件**（`<tempdir>/save/<stem>-<uuid>.part`），
  落地仍由 `commit()` 的流式拷贝负责。**两边都是真实文件系统路径** ⇒ `fs::copy` 成立。
- ⚠️ **本机 `cargo test` 加载即死（`0xc0000139`，与本次改动无关）⇒ 上面两条新单测只过了
  `cargo check --all-targets`（0 错误），未在本机执行**；排查过程见
  [按需取字节立项](2026-09-15-attachment-on-demand-plan.md) §十一.4。**验收判据（RSS 不随文件大小增长）
  也仍未实测**——它要真机跑 2 GB 导出。

### P2b 加密开启时的流式化（**先实测，暂不动格式**）

**文件**（真要做时）：`src-tauri/src/attachments.rs`（**导入** `:568-575`）、
`src-tauri/src/sync.rs`（下载 `:1654-1677`、上传 `:1590-1600`）、
`src-tauri/src/security.rs`（分块 AEAD + 兼容读）

> ⚠️ **触发条件**：这三条**只在用户开了静态加密时**才整块读（§2.6（2）a/b/c）。
> 未加密时导入走 `copy_and_hash` 流式、上传走 `:1597-1599` 流式、下载分块落盘 ⇒ **本来就是对的**。
>
> ⚠️ **为什么先实测**：流式**静态**加密的前提是**分块 AEAD**，而现有是整块 ⇒ 改格式 =
> **存量附件兼容**（读路径要同时认两种，否则老用户附件全部解不开）。
> **判据：真机 + 开加密 + 1–2 GB 文件，先看是否真的 OOM。没实测就不动格式。**
>
> 另需先确认 `attachments.rs:431-439`（整份交给前端）**是否在大媒体预览的热路径上**。

**下载侧**：现在是「明文书 `.part` → rename → 整块读 → 加密 → 写回」（#10、#11）。
改为**边收边加密写 `.part`**，全部成功后再 `rename` ⇒ **明文从不落盘、内存恒定**：

```rust
// 伪代码：分块读取 → 分块加密 → 直接写 .part
let mut file = tokio::fs::File::create(&tmp).await?;
let mut enc = match &session_key { Some(k) => Some(StreamEncryptor::new(k)?), None => None };
while let Some(chunk) = stream.next().await {
    let chunk = chunk.map_err(|e| e.to_string())?;
    size += chunk.len() as i64;
    let out = match &mut enc { Some(e) => e.push(&chunk)?, None => chunk.to_vec() };
    file.write_all(&out).await?;
}
if let Some(e) = enc { file.write_all(&e.finish()?).await?; }   // 收尾（AEAD tag）
std::fs::rename(&tmp, &path)?;                                   // 只有完整才改名
```

**⚠️ 这条有两个必须处理的约束（否则是数据损坏级的 bug）**：
1. **必须确认现有 `encrypt_attachment_bytes` / `decrypt_attachment_bytes` 的格式是"整块 AEAD"还是
   "分块可拼接"**（`nonce||ct`）。若是整块 AEAD，**不能**简单分块加密拼起来——
   要么改成**分块 AEAD（每块独立 nonce + tag）并同时升级读路径**。
   ⚠️ 注意「流式读 → 整块加密 → 流式写」**解决不了问题**：整块加密必然要求整个明文同时在内存里，
   所以它**不降低峰值内存**，只是把"读回"换了个说法——**别做这种看起来流式的假优化**。
2. **改格式 = 存量数据兼容问题**：已有附件是按旧格式存的，读路径必须**同时支持两种**
   （按长度 / 魔数判别），否则老用户的附件全部解不开。
   **这是本方案里风险最高的一步，必须单独一个提交 + 单独的回归证据。**

> **因此 P2b 的诚实结论**：**要么改格式（并做存量兼容），要么就先不做。**
> 动工前的第一件事是确认 `security.rs` 的实际格式（本次**未读**，见 §七）。
> 若格式是整块 AEAD 而不愿承担兼容成本，**P2b 就降级为"只记录、不实施"**——
> 诚实地说"这条暂时不动"，好过做一个假的流式优化。

### P3 同步预算刹车

**文件**：`src-tauri/src/sync.rs`（Modify: `sync_attachments`）、`src/lib/platform/web.ts`（同构修改）、
设置项落在同步 profile 或全局设置

**三档刹车，全部要"优雅停止 + 报告"，不是抛错**：

| 刹车 | 触发条件 | 行为 |
|---|---|---|
| **磁盘余量**（硬性，不可关） | 剩余空间 < 阈值（默认建议 1 GB，待拍板） | **立即停止下载**，保留已下部分，报告"因磁盘余量不足停止" |
| **本次下载总量** | 本轮已下载字节 > 上限（默认值待拍板） | 停止，报告"已达本次上限，可再次同步继续" |
| **单文件大小** | 单文件 > 阈值 | 跳过该文件，计入报告（**这就是 P4 的雏形，但先不引入"未下载"状态**） |

**实现要点**：
- **幂等可续**：因为按内容寻址、且 `local_set` 每轮重算，**停止后再同步会自然接着下** ⇒
  不需要断点续传协议，这是现有设计的免费红利。
- **必须让 #12 的错误处理对称**：把 `:1660` 的 `?` 改成与 `:1646` 一致的「记一笔、跳过、继续」，
  否则一次网络抖动会炸掉整轮（也就无法"优雅停止"）。
- **报告走 P1-b 的通道**，并在 `SyncPanel` 的结果文案里带上"跳过 N 个附件（原因）"。

**验收**：
```bash
# 1) 正常空间不受影响
pnpm test && pnpm release:preflight --skip-remote
# 2) 用一个小配额磁盘/目录模拟余量不足 → 同步应「停止并报告」，而不是报错崩溃
# 3) 连续两次同步 → 第二次接着下，不重复下载已完成的部分（幂等）
```
**期望**：停止后再次同步可继续；`attachments` 表不出现重复行；应用不崩。

### P4 大附件不自动下载 + 「未下载」状态（第二批）

**动工前必须先回答（本次未查，属实施前调研）**：
1. 附件字节缺失时**读路径**现在会发生什么（预览/导出/`pdf://` 回链）？
2. `cleanup_orphan_attachments`（`storage.rs:208,243`）会不会把"只有元数据、没有字节"的记录当孤儿清掉？
3. `FileManagerView` / `AttachmentPanel` 如何表达"未下载、点此下载"？
4. 跨设备：A 机器有字节、B 机器没有——**这是允许的状态，还是需要同步这个事实本身**？

**只有这四问答完，才谈得上写代码**；否则会把"看起来坏了"的体验推给用户。

### P5 网络与时机闸门（待评估）

**前置**：`src-tauri/src` 全仓无网络类型能力（#9）。需要确认：
Tauri 2 是否已有可用插件给出"是否计量网络 / 是否 Wi-Fi"（Android 侧需 `ConnectivityManager`）。
**没有这个能力，本档直接不做**——不要用"UA 或平台猜测"代替真实网络状态（那正是
`platform/index.ts:35-38` 警告过的那类近似错误）。

---

## 六、验收清单

| # | 判据 | 期望 |
|---|---|---|
| 1 | 同步相关**不再有**"是 stub / 没实现"的**断言**（`grep` 的合理残留只有两类：引用被纠正的说法、`capabilities.ts` 里讲 email 的） | 见 **B1** |
| 2 | 桌面手动同步一次后看面板 | 显示"同步完成：上传 X / 拉取 Y"，**不是**"正在同步…"（**B2**） |
| 3 | 桌面同步含多附件的空间 | 出现 `N/M` 与进度条（**B2**） |
| 4 | 导出 / 下载一个 2 GB 附件（走 `FileManagerView.tsx:538` 的「下载」） | **未加密**：**RSS 不随文件大小增长**（**P2a**）；**开了加密**：不 OOM（**P2b**） |
| 5 | 下载中途抓磁盘（**加密开启**） | **不存在**未加密的中间态文件（**P2b**） |
| 6 | 存量附件（旧格式） | 仍能正常打开（**P2b** 兼容性回归） |
| 7 | 磁盘余量不足时同步 | **停止并报告**，不抛错、不崩（**C1** ✅ 代码已落，待真机实测） |
| 8 | 停止后再次同步 | 接着下，不重复（**C1** 幂等：按内容寻址 + 每轮重算差集 ⇒ 天然成立） |
| 9 | 网络抖动（中途断流） | **不再炸掉整轮**（**C1** ✅ 已把上传/下载两侧的 `?` 改成"记一笔、跳过"，件数进 `attachments_failed`） |
| 10 | **B4**：`sync-regression.mjs` 两设备跑一遍 | ✅ **已改判**：这条由 schema + INSERT 唯一决定 ⇒ **代码取证判定成立**并已修（§2.6（3））。真机观感确认仍未做；CI 的两设备脚本走 Web 引擎、复现不出（Rust 独有） |
| 11 | `pnpm run build` / `pnpm test` | 无回归 |

---

## 七、边界与不做的事 / 未验证清单

**不做**：
- **不改"同步会拉附件"这个语义**（那已**另立项目**：P6 ⇒ [按需取字节立项](2026-09-15-attachment-on-demand-plan.md)）。**本方案只加刹车与可见性。**
- **不给 Web 版"加上同步"或"去掉同步"**——它**本来就有实现**，本方案只修口径（P0）+ 保持置灰。
- **不引入"未下载"状态**（P4 之前的任何步骤都不该偷偷引入一个半成品状态）。
- **不动服务端的 2 TB 默认上限**（#23）——那是"本地优先、不武断设限"的既定选择；
  要改也应该是**客户端刹车**，而不是服务端挡人（自建用户的磁盘他自己说了算）。
- **不用 `--skip-remote` 之类绕过**来"验证"P3；余量不足必须真的用小配额环境测。

**未验证（别当已证）**：
1. **没有在真机 / 真服务端上跑过大文件同步**——全部结论来自读代码。要坐实至少两件事：
   ① **默认配置（未开加密）下"填满设备"会做到什么程度**——这是**主要**风险；
   ② **开了加密之后，大文件是否真的 OOM**——这是 **P2b** 的判据（见第 2 条）。
2. **`security.rs` 的附件加密格式本次未读**（`encrypt_attachment_bytes` / `decrypt_attachment_bytes`）——
   只从**调用点与测试**推断出"key 为 `None` 时原样返回"。
   **这是 P2b 的动工前置，但不是 P2a 的**（P2a 不碰格式，可以直接做）。
3. **附件字节缺失时的读路径行为未查**（P4 的前置问题 1、2）。
4. **Android 系统级限制未查**：应用私有目录配额、后台限制、Doze 对 5 分钟定时器的影响。
5. **浏览器配额上限未测**（P0 提到的那条 Web 路径）。
6. **`docs/SYNC.md` 未通读**（只精读了附件那一节），不排除别处已讨论过"选择性同步"。
7. **§2.6（3）那个"第二台设备多出重复行"没有实测**——只有代码依据
   （`sync.rs:1297-1313` 插一行带 `page_id` 的、`sync.rs:1681-1687` 又插一行 `page_id = NULL` 的新 uuid）。
   **必须用 `scripts/sync-regression.mjs` 或 `docs/sync-multidevice-test.md` 验一次**，
   因为如果成立，它是**用户可见的脏数据**（根目录冒出 `hash.ext` 命名的文件），够单独立项。
8. **`attachment_bytes`（`attachments.rs:431-439`）是否在大媒体预览的热路径上未核实**——
   若在，则"打开一个大视频"也会整份进内存；若走 `attachment_path` + 资源协议则不会。**这是 P2b 的前置问题之一。**

---

## 八、收益与代价

| 档 | 收益 | 代价 |
|---|---|---|
| P0 | 消掉"文档与代码相反"的第四例；防止有人把真实存在的 Web 实现当死代码删掉 | 无（纯文档） |
| P1 | 用户第一次**看得见**同步在干什么、有没有结束 | 一处 `end()` + 一条进度事件通道 |
| P2a | 消掉**唯一无条件**的整块读（导出 / 另存为，`FileManagerView.tsx:538` 的「下载」） | 十几行、**无格式变更**；未加密时改走 `fs::copy` |
| P2b | 消掉**加密开启时**的内存与明文窗口（导入 / 上传 / 下载） | **全方案风险最高**：需分块 AEAD ⇒ 存量格式兼容 + 单独回归；**先真机实测再决定，可能永远不做** |
| P3 | 最严重的三条伤害（填满盘 / 不知情 / 抖动炸整轮）**同时**被兜住 | 需要拍板默认阈值；`?`→跳过 会改变错误呈现（要配 P1 的报告） |
| 不做（P7） | 成本为零 | 手机端同步一个大空间 = **静默把设备塞满或被杀**；且**没人会发现**（#16–#20） |

---

## 九、待拍板

按 §4.1 的批次划分，原先 7 条**收敛为 2 条真正需要你定的**——**两条都已裁定（2026-09-15）**：

1. ✅ **C1（预算刹车）的默认阈值** —— **已裁定**：磁盘余量下限 **1 GB（硬性、不可关）**；
   **单文件跳过阈值 100 MB**；本次下载总量上限 **`0` = 先"只报告不拦截"**（先拿数据，再定硬数字）。
   已落到代码（`sync.rs` 的 `DEFAULT_*`）并由单测 `budget_defaults_match_the_decided_numbers` 钉住，
   面板里三项都可调（磁盘余量下限最小 256 MB，**没有"关闭"选项**）。见 §4.3。
2. ✅ **C2 网络闸门本轮做** —— **已做**（发布者裁定）：默认**开**（仅 Wi-Fi 下自动同步），
   Android 上走真实 `ConnectivityManager` 查询，见 §4.3。
3. ~~**P6 要不要立项**~~ **已立项（2026-09-15，发布者裁定）** ⇒ 见
   [附件同步「按需取字节」立项](2026-09-15-attachment-on-demand-plan.md)，
   **先只做"每空间开关"那一半**（P6.1 已落地，见该立项书 §十一）。

**其余 5 条已有明确处置，不需要单独拍板**：

| 原待拍板项 | 处置 |
|---|---|
| P2 做不做 | **拆成 P2a（做，第一批 B3）/ P2b（先实测、暂不动格式）**，见 §4.1 |
| P5 是否引入新依赖 | ✅ **已评估（2026-09-15）：不引入任何依赖** —— 走仓内已有的 Android 原生插件（`ShuyoFsPlugin` + `scripts/android-mobile-shell.mjs`）加一条命令即可拿到真实网络类型，见 §4.2；**只剩"本轮做不做"这一个决定**（C2） |
| 社区仓核对表 #40 改不改 | **已随 B1 一并改**（跨仓，另一次提交） |
| §2.6（3）的"重复行" | ✅ **已判定成立并已修（B4，2026-09-15）**：由 schema + 两条 INSERT 唯一决定，取证即可判定（§2.6（3））；修法 = 兜底行"有就不插" |
| §2.6（1）"同名版本"要不要当真特性 | **属产品口径问题，本方案不承接**（要当真特性是数据模型改动）；**至少写进对外口径**——已在社区仓核对表 #7 收紧 |
