// wire 载荷里 CRDT 状态字段的**版本标记**。
//
// 为什么单开一个文件：同步路径上有**三方**要认同一个数字 —— 客户端发送（`recordChange`）、
// 客户端接收（`applyChange`）、服务端存储/判定（`shuyonote-sync-server`）。
// 写在一个文件里，接线时不会各写一个字面量（本仓对"同一常量两处字面量"的教训已经不止一次）。
//
// 约定：
//   · 载荷里**没有**这一项 ⇒ 老载荷 / 这一页还没有 CRDT 状态 ⇒ 走今天那条路（如实降级）；
//   · 有这一项但 `v` 不认识 ⇒ **不猜**，如实报出来（猜错＝静默丢块）。
export const CRDT_WIRE_VERSION = 1;
