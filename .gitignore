# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-web
*.local

# Transient transpile dir used by scripts/smoke-web.mjs
.smoke-tmp

# Generated pdf.js runtime assets (copied by scripts/copy-pdfjs-assets.mjs)
public/pdfjs

# Generated tesseract offline OCR assets (copied by scripts/copy-tesseract-assets.mjs)
public/ocr

# Generated web deploy version marker (scripts/write-version-json.mjs)
public/version.json

# Local scratch / CDP test temp + Edge profiles (never committed)
tmp
demo-shots

# 商业 / 运营材料绝不进本公开仓库（AGPL 客户端），它们属于私有的
# shuyonote-sync-server 仓库。显式挡一层，防止 `git add -A` 再次误收
# （2026-09-01 已发生过一次：6 份付费客户/值班/工单文档被误推到公开仓库）。
docs/operations-*.md
docs/customer-facing-playbook.*
docs/duty-roster*
docs/zammad-*.md
scripts/export-customer-playbook.mjs

# Rust build artifacts
src-tauri/target

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 临时 askpass 脚本(含密码,勿提交)# 官网临时素材(不入库)
.sshpass*.cmd
site/
regpoc.sh
regok.sh
roundtrip.sh
regcfg.sh
.req.json
